last_session: "2026-04-26T00:00:00+08:00"
current_phase: "phase-5"
current_focus: "Phase 5 正在进行中；Button（多个分类下的 Base 类）当前范围已完成收尾并可冻结，当前默认工作窗口切换为按 active 主计划推进 Input / Card / Modal，并继续定义 Form 输入类、行动类的真实组件范围"
completed:
  - "requirements-alignment — 需求对齐与优化方案确定"
  - "phase-0-repo-scaffold — monorepo 骨架搭建"
  - "north-star-and-roadmap — 四层记忆架构建立"
  - "language-unification — 治理文档统一为中文"
  - "phase-1-exec-plan — Phase 1 详细执行计划产出"
  - "red-team-review — 红队审查改进措施落地"
  - "phase-1-pattern-validation — Button 全链路模式验证完成"
  - "phase-2-component-expansion-and-consumer-validation — 高频组件、contracts、stage、consumer、changesets 完成"
  - "phase-3-real-project-delivery-loop — real-project-1 独立构建与交付闭环完成"
  - "phase-3-standalone-extraction-validation — real-project-1 仓库外独立 install/build/run 验证完成"
  - "phase-4-segment-1-adapters-and-rn-button — adapters 骨架、RN Button 注入边界与最小验证完成"
  - "phase-4-segment-2-stage-baseline-and-weapp-build — stage 多端对照、H5 基线、Weex 策略与小程序最小构建完成"
  - "phase-4-multi-platform-adaptation — Phase 4 全部准出项完成并通过 H5 / 微信小程序运行态验收"
in_progress:
  - "Phase 5 active plan 正作为长期滚动主计划维护，后续组件按批次推进"
  - "Form 展示类已完成首轮 Figma 对齐与 ListItem -> Form 替换，当前已归档；输入类 / 行动类当前仅保留 stage 占位页，待后续定义真实组件范围"
  - "Icon 已落地首轮 Figma 对齐与双源架构（local inline SVG + material font），后续可继续按 registry 扩充本地图标集"
  - "navbar-1 已本地合并 origin/main 并解决 PR 合并冲突；冲突主要集中在 stage 壳层样式、Layout history patch 类型与展示页抽象结构"
  - "apps/stage、apps/example-consumer 与 projects/real-project-1 的 Taro 配置已开启持久化缓存，后续 dev/build 不应再出现 cache Tips"
blocked: []
next_recommended:
  - "以 docs/exec-plans/active/phase-5-token-protocol-and-mapping.md 为唯一主计划，按批次推进 Input / Card / Modal，并在需要时继续定义 Form 输入类、行动类的真实组件范围"
  - "Button / Tag 当前范围仅接受新输入驱动的增量修正；Icon 继续按 registry 做增量扩充"
  - "新增组件或迭代既有组件时，若现有全局 token 不足，先补齐 token 语义再回灌组件实现"
  - "继续按 docs/exec-plans/completed/phase-4-h5-webview-baseline.md 观察 adapters 接入后的 H5 体积变化"
active_exec_plan: "docs/exec-plans/active/phase-5-token-protocol-and-mapping.md"
notes: |
  Phase 4 主准出已完成，当前仓库已进入 Phase 5，且 Phase 5 按“长期滚动执行”治理，而不是一次性收口任务。

  当前正式入口：
  1. 长期协议、组件批次与下一步顺序，以 `docs/exec-plans/active/phase-5-token-protocol-and-mapping.md` 为唯一主计划
  2. 本文件只记录当前工作窗口，不再承载比 active 主计划更细的长期事实

  当前事实快照：
  1. 基础全局样式 token 已完成首轮分层与 Web CSS variables 镜像；后续继续按组件批次补齐语义 token
  2. 当前已完成首轮 Figma 对齐的组件包括 `Button`、`Tag`、`Icon` 与 `Form` 展示类
  3. `Button` 当前准确范围仍仅为多个分类下的 `Base` 类，当前状态为“可冻结”；正式口径为全尺寸 loading spinner、全部尺寸固定高度、`xl=355px` 固定宽度、`l=178px-327px` 区间宽度（当前 stage 按 327px 展示），以及 `xl / l / m` 在流式场景下收缩横向 padding
  4. `Tag` 当前状态为“可冻结”；`Icon` 当前状态为“已落地首轮”
  5. 展示类 `Form` 已完成首轮落地并完成 `ListItem -> Form` 替换；输入类 / 行动类当前仍只保留 stage 占位页
  6. `Text` 不再作为设计系统通用组件保留，stage 中改由 `CssToken 全局样式 / Typography 排版` 承接基础验证
  7. `navbar-1` 已吸收 `origin/main` 的展示类 Form 计划与 stage 兼容样式，PR 合并冲突已在本地解决；Button 页与 `StageShowcasePage` 保留当前分支的抽象展示结构，`Layout` 吸收更严格的 history patch 类型写法
  8. `apps/stage/config/index.ts`、`apps/example-consumer/config/index.ts` 与 `projects/real-project-1/config/index.ts` 已统一配置 `cache.enable: true`，用于开启 Taro / Webpack5 持久化缓存并提升二次编译速度

  当前工作窗口：
  1. 第一优先级是按 active 主计划推进 `Input / Card / Modal`，并在需要时继续定义 `Form` 输入类 / 行动类的真实组件范围
  2. 第二优先级是若新增组件范围暴露 token 缺口，仍遵守“先补 token，再回灌组件”的顺序
  3. `Button / Tag` 当前范围仅接受新输入驱动的增量修正，`Icon` 继续按 registry 扩充

  Stage 与验证约束：
  1. `apps/stage` 的运行态验收以 workspace 包最新 `dist` 为真
  2. 默认使用根命令 `pnpm dev:stage` 进行预览；若改动 `packages/tokens/src`，必须遵守 `tokens build -> components build -> stage 验证` 的顺序，不得并行构建 `tokens/components`
  3. 展示类 `Form` 的变体命名统一使用“通栏 / 卡片”，代码命名统一使用 `flush / card`；旧 `carded` 语义继续保持兼容映射
  4. `FormGroup` / `Form.Group` 已承载列组合消费语义；单独排列与列组合的圆角 / 分割线规则以 `AGENTS.md` 与 active 主计划为准

  已确认治理约束：
  1. TypeScript 仍是 token 与组件合同的主源；Web CSS variables 仅作镜像输出
  2. 组件样式值优先来自本项目全局 token；若当前 token 暂无对应项，必须先询问用户确认，才可临时使用硬编码或 reference token，并在当前任务收尾前完成语义抽取与回灌
  3. 排版协议不保留 `600 / 700` 字重，也不再保留设计系统通用 `Text` 组件
  4. `Card / Modal` spacing 采用“外层容器 padding + 内容行节奏”两级协议，不做单值映射
  5. 状态词口径：阶段层使用“进行中 / 已完成”；组件批次层使用“已落地首轮 / 收尾中 / 可冻结”

  下一个 Agent 进入时：
  - 先读本文件获取当前工作窗口
  - 再读 `docs/ROADMAP.md` 确认 Phase 5 已处于进行中
  - 再读 `docs/exec-plans/active/phase-5-token-protocol-and-mapping.md` 获取长期协议、当前批次与滚动 backlog
  - 若本轮需要在 `apps/stage` 验证 `tokens/components` 改动，默认先运行根命令 `pnpm dev:stage`，不要只启动 `@fdesign/stage`
  - 若改动 `packages/tokens/src`，必须遵守 `tokens build -> components build -> stage 验证` 的顺序，不得并行构建 `tokens/components`
  - 默认优先级：先按 active plan 推进 `Input / Card / Modal` 与 `Form` 输入类 / 行动类范围，再处理新输入驱动的 `Button / Tag / Icon` 增量修正
  - 如未来出现明确 RN 消费方，再重新评估是否恢复更强的 RN 运行态验证
