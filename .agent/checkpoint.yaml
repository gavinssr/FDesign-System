last_session: "2026-04-18T00:00:00Z"
current_phase: "phase-5"
current_focus: "Phase 5 进行中；表单单元 Round 1 / Wave 4 验收通过（2026-04-18，含用户反馈两点回归修复：① form/exhibit/* 全部 cell 宽度按 Figma 固定 card=355/flush=375，由 form/exhibit/_internal/ExhibitFrame 导出 EXHIBIT_FRAME_WIDTH_CARD/EXHIBIT_FRAME_WIDTH_FLUSH 常量统一引用，不再跟随父容器拉伸；② apps/stage/src/pages/form-exhibit/index.css 与 apps/stage/src/pages/form-atoms/index.css 顶部补 /*postcss-pxtransform disable*/ 注释，避免 Taro 按 designWidth=750 把 px 二次缩放约对半；stage form-exhibit 页 __stage-formExhibitPair 由 grid 1fr 1fr 改 flex wrap 适配固定宽 cell），Round 1 整体进入 可冻结（原子层 33 件 + 展示类 13 件 + parts 11 件全部首轮落地并通过 stage H5 运行态视觉/交互验收）；全量验证：tokens build + components test（52 文件 · 71 用例全绿）+ components build + stage build + 两个 consumer build + check-boundaries 全部通过；运行态验证：13 个 cell 的 card=true/false 两态视觉对齐 Figma；ExhibitVeriFace 装饰 SVG 成功/失败两态正确；ExhibitAggregateMultiFold 各子项独立非受控展开；ExhibitInformationListAmount card+expanded 子行自动切 jump（flush+expanded 为 text），与 Figma 规范一致；aria snapshot 校验折叠头 role=button + aria-expanded、jump 类 role=link 语义正确；点击 uncontrolled fold 头可切换并互不影响其它 fold；7 个 nestOrigin local icon 占位 + 5 条 veriFace 装饰 SVG 视觉可接受，未触发 Figma MCP 回灌；Icon.tsx 未扩展 decorative source（保持 Wave 2 既定，单点旁路消费）；当前按用户逐项反馈继续修复 Round 1 原子：已完成第 2 轮 stage 原子页展示协议收口，原子页改为左侧元信息栏 + 右侧预览、括号内 props 名称移到左侧、ErrorText 行移除、NumberTitle 按是否带 arrow 分列展示、InputField / ErrorTips / ErrorPop / Divider 在 stage 上使用设计稿宽度协议而不再随容器拉伸；已继续完成第 3 轮返修：props 名称改为逐个 demo 左侧标注，`InputField` stage 改为 left/right 两列各展示 `wait/typing/filled/disabled` 四态并不再单列 `focus`，`InputField` 删除按钮回到贴近文本的设计稿布局，`ErrorTips / ErrorText / ErrorPop / WordLimit / SelectionContent / TickTag / OptionCard*` 等已优先收敛到现有 semantic token；已完成第 5 轮舞台布局修正：撤回 `form-atoms` 页同组横排等宽等高方案，删除 section 宽度测量逻辑与自适应 grid，`__stage-formAtomsSectionGrid` 改回纵向自动堆叠；已继续完成第 6 轮外层容器拉伸修正：`apps/stage/src/pages/form-atoms/index.css` 将 `__stage-formAtomsSectionGrid` 的 cross-axis 对齐改为 `stretch`，并让 `__stage-formAtomRow` 改为 `width:auto + align-self:stretch`，从而同层外卡片宽度自动沿父容器内边距拉满，但内部 `DemoStack / DemoItem / DemoBody` 等行容器与元素布局保持不变；已继续完成第 7 轮细节修正：`apps/stage/src/pages/form-atoms/index.css` 将 `__stage-formAtomDemoColumnTitle` 的 `padding-left` 从 `84px` 回退为 `0`，使 InputField 分组中的 `left/right` 列标题与本列上下元素直接左对齐；已继续完成第 8 轮原子纠偏：`InputField` 增加 typing 初始聚焦、删除清空、回车/外点失焦与设计稿删除图标；`ErrorPop` 改为 40px 固高、`colors.semantic.translucent.functionalRed.a30` 背景与 neutral 描边，并从 stage 移除 plain 变体；`SelectionContent` 改为占位球 + 文案 + jump 的紧凑片段；`OperationText` 新增 `TextOperator` 别名并将文字按钮颜色/字重收回到 text.primary + 14/16 Medium；`OperationButton` 的 preContent/subText/tagAmount/plusCount/textButton 追加 jump 表达，`OperationButtonHarness` 按 Figma `8440:15495` 重排；jump 类箭头统一改用用户提供的 16x16 SVG 路径并通过 local icon viewBox 承载；本轮验证：`pnpm --filter @fdesign/components test`、`pnpm --filter @fdesign/components build`、`pnpm --filter @fdesign/stage build` 全部通过（stage 仅保留既有 entry size warning）；下一步继续接用户后续修复点。"
completed:
  - "requirements-alignment — 需求对齐与优化方案确定"
  - "phase-0-repo-scaffold — monorepo 骨架搭建"
  - "north-star-and-roadmap — 四层记忆架构建立"
  - "language-unification — 治理文档统一为中文"
  - "phase-1-exec-plan — Phase 1 详细执行计划产出"
  - "red-team-review — 红队审查改进措施落地"
  - "phase-1-pattern-validation — Button 全链路模式验证完成"
  - "phase-2-component-expansion-and-consumer-validation — 高频组件、contracts、stage、consumer、changesets 完成"
  - "phase-3-real-project-delivery-loop — real-project-1 独立构建与交付闭环完成"
  - "phase-3-standalone-extraction-validation — real-project-1 仓库外独立 install/build/run 验证完成"
  - "phase-4-segment-1-adapters-and-rn-button — adapters 骨架、RN Button 注入边界与最小验证完成"
  - "phase-4-segment-2-stage-baseline-and-weapp-build — stage 多端对照、H5 基线、Weex 策略与小程序最小构建完成"
  - "phase-4-multi-platform-adaptation — Phase 4 全部准出项完成并通过 H5 / 微信小程序运行态验收"
  - "phase-5-form-cell-round-1 — Form Cell Round 1（原子组件 33 + 展示类 cell 13 + parts 11）首轮 Figma 对齐并通过 Wave 4 运行态验收，整体可冻结（2026-04-18）"
in_progress:
  - "Phase 5 长期滚动主计划继续维护：phase-5-token-protocol-and-mapping.md（全局 token 协议） + phase-5-form-cell.md（表单单元三轮，当前 Round 1 可冻结，Round 2/3 待启动）"
  - "Button 已落地首轮 Figma 对齐，但当前准确范围仅为多个分类下的 Base 类，仍处于收尾中；可在 Round 2 之间穿插推进"
  - "Icon 已落地首轮 Figma 对齐与双源架构；decorative 分类已注入 5 条 veriFace 装饰 SVG，当前由 ExhibitVeriFace 旁路消费，Icon.tsx 的 source 分支未扩展 decorative（等 Round 2/3 出现多点消费再评估）"
  - "Button 已落地首轮 Figma 对齐，但当前准确范围仅为多个分类下的 Base 类，仍处于收尾中"
  - "Phase 5 长期滚动主计划继续维护：phase-5-token-protocol-and-mapping.md（全局 token 协议） + phase-5-form-cell.md（表单单元三轮）"
  - "Icon 已落地首轮 Figma 对齐与双源架构（local inline SVG + material font），decorative 装饰图分类已注入首批 5 条（veri-face-* / veri-frame-* / veri-scan-bar），当前由 ExhibitVeriFace 旁路消费；Icon.tsx 的 source 分支暂未扩展 decorative（如 Wave 3 之后出现多组件复用装饰图再评估）"
blocked: []
next_recommended:
  - "进入 Form Cell Round 2（Input 类 cell）：在 packages/components/src/form/input/ 下按 Figma 26206:20590 section 的 Input 相关节点落地输入类 cell；每个 cell 配套 .tsx + .types.ts + .contract.yaml + .test.tsx + .harness.tsx + index.ts；card=true/false 仍作为同一组件内 style variant（沿用 Round 1 冻结决议）"
  - "Round 2 直接复用 Wave 1 原子（InputField / ErrorTips / ErrorText / ErrorPop / WordLimit / Label / Divider / OperationButton 的 delete/textButton 变体等），无需回补原子；如 Figma 明确要求新增子件（如带单位输入 / 选择器），按「先补原子再装 cell」执行"
  - "Round 2 的舞台承接：在 apps/stage/src/pages/ 下新建 form-input/ 页，在 componentLinks.ts 的 Form 表单单元 分组中 Exhibit 展示类 之下、Atoms 原子组件 之上追加 Input 输入类 导航项（Atoms 仍居分组末位，冻结决议）；app.config.ts 同步追加 pages/form-input/index"
  - "Round 2 可能触发的 token 扩展（如 focus 态描边宽度 / 输入占位色等），必须先在 phase-5-token-protocol-and-mapping.md 同步协议扩展，再回灌组件实现"
  - "Round 3（Action 类 cell）紧随 Round 2；Round 3 完成后本 exec-plan 可移至 docs/exec-plans/completed/"
  - "Button（Base 类）收尾可在 Round 2 之前/之间穿插推进，但不阻塞 Round 2 主线"
  - "如 Round 2/3 新增 cell 发现 7 个 nestOrigin local icon 或 5 条 veriFace 装饰 SVG 的视觉偏移明显，用 Figma MCP 拉原节点 SVG 后替换 iconRegistry.tsx / decorativeIconRegistry 对应条目的 d / render；不得引入额外图标渲染管线"
  - "如 Round 2/3 出现多点装饰图消费，再评估是否在 Icon.tsx 的 source 分支接入 decorative 分支；当前单点旁路已是稳态"
  - "新增组件或迭代既有组件时，若现有全局 token 不足，先补齐 token 语义再回灌组件实现"
active_exec_plan: "docs/exec-plans/active/phase-5-form-cell.md"
secondary_active_exec_plan: "docs/exec-plans/active/phase-5-token-protocol-and-mapping.md"
notes: |
  Phase 4 主准出已完成，当前仓库已进入 Phase 5，且 Phase 5 按“长期滚动执行”治理，而不是一次性收口任务。

  当前事实快照：
  1. `vars.css` 已落地首轮输入与协议抽取，radius / spacing / typography 规则已沉淀到 `docs/exec-plans/active/phase-5-token-protocol-and-mapping.md`
  2. 仓库已落地基础全局样式 token 的首轮分层与 Web CSS variables 镜像；后续会随组件新增/迭代继续补齐语义 token
  3. 当前已完成首轮 Figma 对齐的组件包括 `Button`、`Tag` 与 `Icon`；其中 `Button` 当前准确范围仅为多个分类下的 `Base` 类 button，不代表全部 button 分类均已完成
  4. `apps/stage` 新壳层与 `Button` 样板页已落地，但 `Button` 当前仍在按展示反馈继续调整；现阶段收尾对象仍以多个分类下的 `Base` 类 button 为主，尚未达到“可冻结”
  5. stage 侧栏分组导航箭头已按 Figma 节点 `25860:3434` 完成一轮图标对齐，当前采用与设计稿一致的 chevron 路径替换原 border 拼接形状
  6. stage 壳层点击热区已收敛为通用 cursor 规则：侧栏导航与控制面板内的可点击项默认使用手型光标，组件本体默认不纳入
  7. 已为未来 stage 非导航类自定义点击区预留 `__stage-clickable` 能力，后续新增热区可显式复用而无需再单点补样式
  8. 一级折叠导航点击规则已细化：折叠态点击仅在当前分组没有已选中二级项时才跳到第一个二级导航；若该分组已有选中项，则仅展开
  9. 一级折叠导航在展开态下点击仍仅折叠，不切换当前二级项
  10. 一级折叠导航展开状态已持久化到 stage 本地存储；切换到非折叠一级导航后不会因为页面重建自动折叠既有已展开分组
  11. 可折叠一级导航的文案与箭头着色已与展开状态解耦；无论展开或折叠都维持未展开态颜色，仅保留 hover 反馈
  12. stage 组件命名的双语展示格式已统一：组件名中的 `English / 中文` 已收敛为 `English 中文`，英文名与中文名之间只保留一个空格
  13. 历史上的“等待 Figma 输入”待机状态已结束；当前唯一 active 计划为 `docs/exec-plans/active/phase-5-token-protocol-and-mapping.md`
  14. stage 全部已接入组件页面的内容区已按 `Base 基础按钮` 页样板统一为“Hero + Gallery 分节”结构；Button 二级页也已补齐同结构占位，未改动 stage 壳层 header、主导航和展示区布局
  15. stage 展示页已移除 `矩阵验证 / Harness Matrix` 模块；舞台页只保留面向展示的内容，不再承载代码层矩阵验证
  16. `Text` 不再作为设计系统通用组件保留；`apps/stage` 已新增折叠一级导航 `CssToken 全局样式`，并将原 `Text 文本` 页重构为其下的 `Typography 排版` 基础验证页
  17. `apps/example-consumer` 与 `projects/real-project-1` 中原先依赖 `@fdesign/components/Text` 的文案已回收为“原生文本元素 + 页面层样式”写法，不再通过通用 `Text` 组件承载排版组合
  18. `packages/components` 的 Vitest 测试基座已补齐统一 `setupFiles`；通过注入最小 Taro mock 与 `ENABLE_INNER_HTML` 全局常量，组件测试已恢复可运行状态
  19. `Icon` 已从字符型 placeholder 重构为双源统一入口：未显式传 `source` 时按“local registry 优先，否则 material”自动分发
  20. `packages/tokens` 已新增 `spacing.component.icon` 尺寸协议；`Icon` 已接入 `special-mini / xxs / xs / s / m / special-large` 六档尺寸、tone/color 覆盖与 Material Symbols 的 `wght/GRAD` 映射
  21. 当前仓库已接入用户提供的 40 个自有导出 SVG，并统一收敛到 `Icon` local registry；旧用法中的 `info / check / chevron-right` 已通过 material source 兼容，且本地图标已完成两轮非破坏式语义别名整理，旧文件名风格名称仍保持兼容
  22. `apps/stage` 的 `Icon 图标` 页已重写为 Hero + Local / Material / Sizes / Tones 分节，且导航位置已移到 Button 分组之后；当前 local 图标区优先展示语义别名，已覆盖卡类、账单/还款、公积金、红包提现等业务命名
  23. `apps/stage` 的通用 `Gallery / 组件总览` 描述文案已从 `ComponentDemo` 头部全局移除，后续所有组件页不再显示该行说明文字
  24. `apps/stage` 的 Icon 展示区已补齐统一 tile 排版：local/material 两区的单项展示容器宽度一致，仅展示单个 display name，超长名称按单词换行；tile 的 top/left/right padding 为 24px，bottom padding 与 icon-label gap 保持一致，且已进一步收紧横向密度
  25. `apps/stage` 的 Icon 页中 `Tones` 分节已收敛为仅展示注释图标 `info`；`Sizes` 分节已改为对 `fd-icon-root` 施加仅 stage 生效的 inside border 说明样式，border 粗细等于尺寸协议中的 padding，并通过预览层放大校正使图形更贴近绘图区边缘；展示名称改为 `mini / xxs / xs / s / m / large`
  26. `apps/stage` 已新增可复用的备注类小组件 `StageNoteText`：默认文案为“备注文案”，当前已在 Icon 页 `Material / info` 尺寸卡标题旁落地“外部组件绘图区不受控”说明
  27. `apps/stage`、`apps/example-consumer` 与 `projects/real-project-1` 已接入 Material Symbols Rounded 字体加载；`pnpm --filter @fdesign/tokens build && pnpm --filter @fdesign/components test && pnpm --filter @fdesign/components build && pnpm --filter @fdesign/stage build && pnpm --filter @fdesign/example-consumer build && pnpm --filter @fdesign/real-project-1 build` 全部通过，仅保留既有 H5 entry size warning
  28. `apps/stage` header 已新增品牌 logo：资源复制到 `src/assets/fd-logo.png`，并在壳层 `Layout` 左侧接入；当前 logo 尺寸已接入 header 样式变量，与标题字号口径保持联动
  29. `apps/stage/src/index.html` 已补齐浏览器页签配置：当前 H5 页签标题为 `FDesign System`，并以内联 data URL 方式接入 favicon，避免额外静态路径耦合
  30. `apps/stage` 一级折叠导航已补充“展开且其下存在当前选中二级项”态：当前通过 `__stage-navLinkGroupActive` 单独上背景色，并已切换为蓝到粉的半透明横向渐变背景
  31. `apps/stage` 的 `CssToken 全局样式` 已统一迁移到 `pages/css-token/*` 路径下，当前导航已补齐 `Typography / Color / Spacing / Radii` 四个二级页
  32. `apps/stage` 已为 token 展示补充通用叶子节点展开与示意预览能力；其中 `Spacing` 页的 component 分节不再借用真实组件，而是使用 stage 私有的蓝色元素示意与浅红标注色矩形来表达横向/纵向间距
  33. `apps/stage` 非折叠导航选中态的左侧高亮已从纯色边框改为蓝到粉的纵向渐变条，当前通过 active 伪元素实现，不改动既有导航交互逻辑
  34. `apps/stage` 侧栏导航已恢复独立纵向滚动：移除了阻断原生滚轮的 `preventDefault()`，并为导航列表补充底部滚动缓冲区；当前最后一个导航项在极限滚动时可上移到可视区垂直中线附近，且滚动不会链到 header、展示区或整页
  35. `apps/stage` 侧栏滚动行为已细化为“仅真实溢出时可滚动”：当前通过运行时测量导航内容高度来切换溢出态；未溢出时隐藏侧栏纵向滚动，溢出时才挂载底部缓冲区；同时滚动条宽度已收窄为上一版的一半
  36. `Tag` 已按 Figma 节点 `26117:11255` 完成一轮组件本体对齐：API 已切换到 `variant / color / couponPrefix`，并新增 `purple` 语义色与 tag 紧凑尺寸协议；`apps/stage` 的 `Tag 标签` 页已改为 `Variants / Colors / Coupon` 三分节展示结构
  37. `apps/stage` 的 `Tag 标签` 页颜色卡片与券式卡片已从通用栅格卡片切换为页面私有的内容自适应容器：当前容器宽度以内部标签实际宽度为基准，左右 padding 对称收口，不再被父级列宽拉伸
  38. `Tag` 当前轮次已通过用户验收：组件本体、token 映射与 stage 展示统一进入“可冻结”，后续仅接受新输入驱动的增量修正
  39. `packages/tokens` 已新增 `colors.semantic.translucent` 语义分组，并接入 `brandBlue / brandPink / functionalRed / functionalOrange / functionalGreen / neutralGray` 的透明度颜色协议
  40. `apps/stage` 的 `CssToken / Color` 页已新增独立 `Translucent` 分节，并补齐 `colors.semantic.translucent.*` 到 `--fd-color-translucent-*` 的 CSS Var 映射展示
  41. `packages/tokens` 已按用户确认执行 `PR-2 + PR-3`：补齐 Typography 原子缺失镜像（display xlarge/xxlarge、single/body 相关 line-height 与 family semantic），并新增 `typography.styles` 与“仅当 fontFamily/fontSize/lineHeight/fontWeight 全量完全一致时命中”的复合样式匹配机制
  42. Cursor Cloud Agent：已补齐 `.cursor/environment.json`（pnpm 固定版本 + frozen install）、对齐 `.cursor/worktrees.json`，并在根 `package.json` 增加 `pnpm.onlyBuiltDependencies`，避免 pnpm 10 默认拦截 Taro/esbuild 等依赖的 postinstall 导致云端构建不完整；已从 git 中移除误跟踪的各应用 `.swc` 插件缓存文件；相关提交已合并进 `dev-listCell` 并推送远端
  43. 已在 Cursor Cloud 机器完成开发环境实测：`pnpm install` 后分别启动 `@fdesign/stage`、`@fdesign/example-consumer` 与 `@fdesign/real-project-1` 开发服务，当前监听端口为 `10086/10087/10088`，并已完成浏览器运行态验收
  44. Form Cell Round 1 计划已用户裁决冻结（2026-04-18）：B.1 通过 `surface.iconPlaceholder='#edf3fa'`、veriFace 5 装饰色不入 token；B.2 全部 8 个 typographyStyles 复合样式新增（含 `displayNumber14Medium`）；B.3 选 A——letterSpacing 不入 token，组件内常量；B.4 选 A——`spacingScale[6]=6` 落地 + `spacingSemantic.gapLabelToSubLabel=6`；B.5 默认通过，`spacingComponent.exhibit` 落地 10 个语义字段；OperationButton 11 种 type 命名保留 Figma 原命名；veriFace 装饰 SVG 进 Icon registry 的「decorative」新分类（与既有黑白 outline 分类并列），不参与 outline 的统一描边/着色协议
  45. 表单单元交互协议（全局约束）：移动端表单单元禁用 hover 态样式与默认 cursor:pointer；折叠类内置受控+非受控双模式；jump 类用 onJump
  46. Form Cell Round 1 / Wave 0 已完成（2026-04-18）：tokens 落地 `colors.semantic.surface.iconPlaceholder` / `spacingScale[6]` / `spacingSemantic.gapLabelToSubLabel` / `spacingComponent.exhibit`（10 字段）/ `fontSize.displayMicro=20` / `lineHeight.singleLine.displayMicro=22` / 8 个新复合 typographyStyles，全部镜像到 `packages/tokens/src/web-vars.css`；`iconRegistry.tsx` 新增 7 个 nestOrigin local 图标（当前为 Material 风格几何路径占位）+ 全新 `decorative` 分类架构（`DecorativeIconDefinition` 类型 + 空注册表 + 类型守卫，等 Wave 2 注入 SVG）；`packages/components/src/list-item/`、`apps/stage/src/pages/list-item/` 完整删除，`packages/components/src/index.ts` / `apps/stage/src/app.config.ts` / `apps/stage/src/shell/componentLinks.ts` 移除导出与导航项；`apps/example-consumer` 与 `projects/real-project-1` 中 ListItem 用法已回收为「原生 View + Icon」组合；`pnpm --filter @fdesign/tokens build` / `@fdesign/components test+build` / `@fdesign/stage build` / `@fdesign/example-consumer build` / `@fdesign/real-project-1 build` 全部通过
  47. Phase 5 协议扩展（同步落地 phase-5-token-protocol-and-mapping.md）：spacing 协议小节「基础 scale」已扩到 `0/6/8/10/12/16/32/40`，并标注 `6` 为表单单元扩展档位（仅限 multiLine 内 title↔subLabel 同档紧凑场景）；typography 协议小节字号体系已扩到 `10/12/14/16/18/20/22/26/36/44`，并标注 `20` 为表单单元扩展档位（仅限 `displayNumber20Medium` 等数字展示场景）；其它场景仍按既有档位选用，避免协议漂移
  48. Form Cell Round 1 / Wave 1 已完成（2026-04-18）：`packages/components/src/form/` 骨架建立（`atoms/` + `exhibit/parts/` + `form/index.ts` + `atoms/index.ts`）；共 33 个原子组件落地，每个原子均配套 `.tsx` + `.types.ts` + `.contract.yaml` + `.test.tsx` + `.harness.tsx` + `index.ts`，覆盖五组：Label 家族 10（Label / NumberTitle / BrandColor / LabelMultiTextFirst / LabelMultiTextSecondaryDark / LabelMultiTextSecondaryLightColor / LabelAnnotation / LabelDescrip / LabelResultSupple / LabelAmountSupple）、Input & Error 家族 5（InputField / ErrorTips / ErrorText / ErrorPop / WordLimit）、Action 家族 6（OperationText / OperationButton / Jump / Collapse / Supplement / LoginMethods）、Selection 家族 9（SelectionContent / Checkbox / Radio / Tick / TickTag / OptionCardDefault / OptionCardMultiLabel / OptionCardPic / OptionCardThumb）、Misc 通用 3（Switch / Divider / PlaceholderIcon）；新增两个内部共享原语 `form/atoms/_internal/textPrimitive.tsx`（FormText）与 `form/atoms/_internal/selectionBox.tsx`（SelectionBox，square/circle/tick 三形），以及一次性批量生成脚本 `scripts/_form-atoms-gen.mjs`；`packages/components/src/index.ts` 新增 `export * from './form'`；`packages/tokens/src/index.ts` 补齐 `TypographyStyleKey / TypographyStyleSpec` 类型再导出；`apps/stage` 新增 `pages/form-atoms/` 页并在 `app.config.ts` 与 `shell/componentLinks.ts` 接入 `Form 表单单元` 分组（当前仅含 `Atoms 原子组件` 一页，位于分组最下方）；所有 OperationButton 的 11 种 type 沿用 Figma 原命名；所有可交互原子（Switch / Jump / Collapse / Supplement / OperationButton / OptionCard 家族等）严格遵循移动端协议（无 hover、无默认 cursor:pointer）；`pnpm --filter @fdesign/components tsc --noEmit / test / build`（39 测试文件 · 53 用例全绿）、`pnpm --filter @fdesign/stage build`、`pnpm --filter @fdesign/example-consumer build`、`pnpm --filter @fdesign/real-project-1 build`、`pnpm check-boundaries` 全部通过
  49. Form Cell Round 1 / Wave 2 已完成（2026-04-18）：`packages/components/src/form/exhibit/` 落地 8 个展示类 cell，每个 cell 均配套 `.tsx` + `.types.ts` + `.contract.yaml` + `.test.tsx` + `.harness.tsx` + `index.ts`——SingleLine 2（ExhibitSingleLineLabel 12287:12197 / ExhibitSingleLineLabelPreContent 8275:8466）、MultiLine 5（ExhibitMultiLineDefault 8333:12737 / ExhibitMultiLinePretext 8275:8467 / ExhibitMultiLineNumericTitle 8275:8468，Roboto Medium 18/20 紧缩 -0.625px / ExhibitMultiLineRightMultiPretextA 8275:8469 / ExhibitMultiLineRightMultiPretextB 8275:8824）、Verification 1（ExhibitVeriFace 8333:12497，固定 card，veriFailed 两态）；`card=true/false` 作为同一组件内 style variant，由新增的共享内部片段 `form/exhibit/_internal/ExhibitFrame.tsx`（card/flush 两态 + 可选 bottom hairline 通过 inset box-shadow）和 `form/exhibit/_internal/exhibitParts.tsx`（含 ExhibitTitleInline / ExhibitDefaultBlueTag / ExhibitLeading / ExhibitLeadingTitle / ExhibitMultiLineStack / ExhibitPresetText / ExhibitRightPrimary / ExhibitRightSecondary）统一承接；`_internal/*` 不对外导出；`form/exhibit/index.ts` 按目录结构导出 8 个 cell，`form/index.ts` 追加 `export * from './exhibit'`；`iconRegistry.tsx` 的 `decorativeIconRegistry` 落地首批 5 条装饰 SVG（veri-face-success 36×36 / veri-face-failed 36×36 / veri-frame-success 100×100 / veri-frame-failed 100×100 / veri-scan-bar 100×8 横向三段渐变扫描条，success/failed 共用），全部写死 fill/stroke（不读 currentColor），veriFace 5 装饰色作为 iconRegistry 和 ExhibitVeriFace 组件内部常量（B.1 决议）；`ExhibitVeriFace` 采用"旁路 Icon 组件"方案直接调用 `getDecorativeIconDefinition` 并以原生 `<svg>` 渲染装饰图，`Icon.tsx` 的 source 分支暂不扩展 `decorative`（避免 outline 尺寸协议与装饰彩色耦合）；`apps/stage/src/pages/form-exhibit/` 新建（config + css + tsx）并在 `app.config.ts` 与 `shell/componentLinks.ts` 接入 `Exhibit 展示类` 导航项，位列 `Form 表单单元` 分组中 `Atoms 原子组件` 之上（Atoms 仍居末位，冻结决议）；舞台页三分节（单行展示类 / Single Line、多行展示类 / Multi Line、人脸验证 / Verification），每个 cell 以 2 列 CellPair 同时并排渲染 card=true 与 card=false 两态；容器背景统一使用 surface.page；所有 cell 与 ExhibitFrame 严格遵守移动端协议（无 hover、无默认 cursor:pointer）；`pnpm --filter @fdesign/tokens build`、`pnpm --filter @fdesign/components tsc --noEmit`、`pnpm --filter @fdesign/components test`（47 文件 · 64 用例全绿，较 Wave 1 新增 8 个 exhibit 测试文件 · 11 个新用例）、`pnpm --filter @fdesign/components build`、`pnpm --filter @fdesign/stage build`、`pnpm --filter @fdesign/example-consumer build`、`pnpm --filter @fdesign/real-project-1 build`、`pnpm check-boundaries` 全部通过
  52. Form Cell Round 1 / Wave 4 · 验收回归修复（2026-04-18）：用户在 stage 运行态发现两个问题——① 表单单元 cell 宽度跟随父容器拉伸而非按 Figma 固定；② stage form-exhibit / form-atoms 页内 row label / pair caption 文字过小（实际渲染 ~6px）。本次闭环修复：① `form/exhibit/_internal/ExhibitFrame.tsx` 新增导出 `EXHIBIT_FRAME_WIDTH_CARD = 355` / `EXHIBIT_FRAME_WIDTH_FLUSH = 375` 两个常量；ExhibitFrame 外层按 card prop 切固定宽 + flexShrink:0 + boxSizing:border-box；ExhibitVeriFace / ExhibitAmountListTitleExternal / ExhibitAggregateMultiFold 三件固定 card 改用 EXHIBIT_FRAME_WIDTH_CARD（355）；ExhibitInformationList / ExhibitInformationListPlainText / ExhibitInformationListAmount 三件按 card prop 在 EXHIBIT_FRAME_WIDTH_CARD/FLUSH 之间切；parts/* 内部嵌套原子保持 width:100% 作为 cell 内填充；② `apps/stage/src/pages/form-exhibit/index.css` 与 `apps/stage/src/pages/form-atoms/index.css` 顶部各补 `/*postcss-pxtransform disable*/` 注释（与 stage 全局 app.css / shell/styles/layout.module.css 同一约定；后者通过 var() 引用 disable 后的 CSS variable 间接受益，page-level 直接写 px 必须补此注释否则被 Taro pxtransform 按 designWidth=750 二次缩放约对半，12px font-size → 实际 ~6px）；stage form-exhibit 页 `__stage-formExhibitPair` 由 `display: grid; grid-template-columns: 1fr 1fr` 改 `display: flex; flex-wrap: wrap; align-items: flex-start; gap: 16px`，列宽不再约束 cell；`__stage-formExhibitPairItem` 加 `flex: 0 0 auto`；治理决议追加：「表单单元组件本体宽度固定 Figma 设计稿，不暴露为 prop，不响应式自适应」+「stage page-level CSS 文件首行必须补 /*postcss-pxtransform disable*/ 注释」两条作为 Round 2/3 沿用约束；全量验证（tokens build / components tsc + test 52 文件 71 用例 + build / stage build / 两个 consumer build / check-boundaries）全部通过；stage H5 dev server 运行态复验：caption 文字恢复 12-14px 可读高度，13 个 cell 在 flex-wrap 容器中固定宽度并排展示，flush 明显宽于 card，三件固定 card 单实例展示也是固定 355
  51. Form Cell Round 1 / Wave 4 · 验收已完成（2026-04-18）：Round 1 整体进入 "可冻结"。全量自动验证：pnpm --filter @fdesign/tokens build、pnpm --filter @fdesign/components test（52 文件 · 71 用例全绿）、pnpm --filter @fdesign/components build、pnpm --filter @fdesign/stage build、pnpm --filter @fdesign/example-consumer build、pnpm --filter @fdesign/real-project-1 build、pnpm check-boundaries 全部通过。stage H5 dev server 运行态（http://localhost:10086）视觉 + 交互验收：13 个展示类 cell 在 Single / Multi / Verification / Combined / Foldable 五分节下 card=true/false 两态并排对照正常；ExhibitVeriFace 成功/失败两态装饰 SVG 结构与色彩正确；ExhibitAggregateMultiFold 首项 defaultExpanded=true 展开 SubInnercard，其它子项折叠，各自非受控状态互不干扰；ExhibitInformationListAmount 在 card+expanded 下子行自动切为 jump（action-jump 12px 箭头 + 浅色 tertiary 文本），flush+expanded 下子行保持 text（深色 primary），差异对齐 Figma 规范；通过浏览器 aria snapshot 校验所有折叠头 role=button + aria-expanded、jump 类 role=link 语义正确；点击 uncontrolled fold 头部可在 collapsed 与 expanded 之间切换，不影响其它 fold 实例。视觉偏移评估：7 个 nestOrigin local icon 的 Material 风格几何路径占位（camera / placeholder-icon / supplement-annotation / supplement-jump / collapse-on / collapse-off / action-jump）与 decorativeIconRegistry 的 5 条 veriFace 装饰 SVG 均视觉可接受，未触发 Figma MCP 回灌（保留为 Round 2/3 出现明显偏移时 reactive 处理）。Icon.tsx 的 source 分支保持未扩展 decorative（当前单点旁路消费，避免 API 面膨胀）。Round 1 整体进入 "可冻结"：原子层 33 件 + 展示类 cell 13 件 + parts 11 件全部首轮 Figma 对齐并通过运行态验收；后续仅接受新输入驱动的增量修正
  50. Form Cell Round 1 / Wave 3 已完成（2026-04-18）：`packages/components/src/form/exhibit/parts/` 落地 11 个内部嵌套原子（GroupTitleH1 8275:8525 / SubCellFlush 8275:10360 / SubListFlush 8275:9193 / SubListCard 15875:9901（委托 SubListFlush + card=true）/ FoldingPureHeader 18707:10950（含 rightNumeric 切换 Roboto 数字字体族）/ CellAmountTag 8417:9026（Roboto Medium 20/22 紧缩字距 -0.625px）/ AmountList 8997:28837（rounded 4 overflow:hidden 容器，末行关闭 hairline）/ ExternalTileAmount 8417:8155（paddingL 2 + gap 4 横向标题行，含 BrandColor Small 品牌色金额）/ SubInnercard 8417:3847（首/尾行圆角 + 尾行 pb 12）/ CollapseNestAmount 8417:4085（受控 expanded + 非受控 defaultExpanded + onToggle 双模）/ AmountNestList 8987:24897），全部仅配 `.tsx + index.ts`，不配 harness/contract/test（Wave 1 原子与 Wave 2/3 cell 的测试/harness 已覆盖其组合行为），聚合出口在 `form/exhibit/parts/index.ts` 供 exhibit 内部消费，**不**在 form/exhibit/index.ts 或 form/index.ts 中对外导出（冻结决议）；`packages/components/src/form/exhibit/` 落地 5 个组合/折叠 cell（每个均配套 .tsx + .types.ts + .contract.yaml + .test.tsx + .harness.tsx + index.ts）——ExhibitInformationList（8275:10982，GroupTitleH1 + SubListFlush，card 双态 + 可选 operation/onOperationPress + 子行 text/jump 默认切换）、ExhibitAmountListTitleExternal（8417:9331，ExternalTileAmount + AmountList，固定 card，外层 flex-col gap 12）、ExhibitAggregateMultiFold（8417:5824，固定 card，52 高标题行 16/18 Medium + 12 tertiary 计数按 items.length 自动推导 "共N笔" + AmountNestList，可选 onJump role=link，每项独立非受控 defaultExpanded）、ExhibitInformationListPlainText（8275:8492，card × expanded 双态，受控 expanded + 非受控 defaultExpanded + onExpandChange 双模，展开态 FoldingPureHeader + SubListFlush text）、ExhibitInformationListAmount（8275:8513，card × expanded 双态，FoldingPureHeader rightNumeric Roboto Medium 14/16，**card+expanded 子行自动切为 jump（浅色 tertiary + 12px action-jump 右箭头）**，flush+expanded 子行为 text）；`form/exhibit/index.ts` 追加 5 cell 导出；FoldingPureHeader 不对 chevron 施加 rotate（collapse-on/off local icon 路径已编码方向，直接切换 name 避免与 atoms/Collapse 的 transform 歧义，代价是无过渡动画）；GroupTitleH1 的"操作文本"按 Figma 规范是 12 Medium primary-blue（与 atoms/OperationText 14 regular 不同），因此不复用 atom 而直接用 FormText + body12SingleLineStrong + action.primary.background 渲染；ExhibitInformationListAmount 的 card+expanded 态子行切 jump 是 Figma 规范明文差异（card 语境下展开二级行隐含跳转意图），作为 cell API 内置行为固化而非暴露为 prop；apps/stage 的 Exhibit 展示类 页在 Wave 2 三分节基础上追加 组合展示类 / Combined 与 折叠展示类 / Foldable 两节；heroMeta 更新为 Cells=13 / Parts=11（不对外）/ controlled+uncontrolled+onJump；Atoms 仍居 Form 分组末位（冻结决议），不拆子页；全量 pnpm --filter @fdesign/components exec tsc --noEmit、test（52 文件 · 71 用例全绿，较 Wave 2 新增 5 个 exhibit 测试文件 · 7 个新用例）、build、apps/stage build、两个 consumer build、check-boundaries 全部通过

  已确认治理约束：
  1. TypeScript 仍是 token 与组件合同的主源；Web CSS variables 仅作镜像输出
  2. 组件样式值优先来自本项目全局 token；若当前 token 暂无对应项，必须先询问用户确认，才可临时使用硬编码或 reference token，并在当前任务收尾前完成语义抽取与回灌
  3. 排版协议不保留 `600 / 700` 字重，也不再保留设计系统通用 `Text` 组件
  4. `Card / Modal` spacing 采用“外层容器 padding + 内容行节奏”两级协议，不做单值映射
  5. 状态词口径：阶段层使用“进行中 / 已完成”；组件批次层使用“已落地首轮 / 收尾中 / 可冻结”

  下一个 Agent 进入时：
  - 先读本文件获取当前工作窗口
  - 再读 `docs/ROADMAP.md` 确认 Phase 5 已处于进行中
  - **必读** `docs/exec-plans/active/phase-5-form-cell.md` 的「计划状态」「决议总览（冻结版）」「执行 Wave 划分」「组件映射表」四段——**Round 1 已全部完成并通过 Wave 4 验收进入「可冻结」**；当前主任务为 Round 2（Input 类 cell）
  - 同步读 `docs/exec-plans/active/phase-5-token-protocol-and-mapping.md` 的 spacing 与 typography 协议小节，确认 `6` 与 `20` 已分别作为扩展档位落地；Round 2 如需扩展 token（例如 focus 态描边宽度 / 输入占位色），必须先在此协议文件同步扩展，再回灌组件
  - 默认优先级：直接进入 Form Cell Round 2；不要再回头讨论已冻结的 Round 1 范围/目录/API/token 候选/交互协议；Round 1 的原子层 33 件 + 展示类 cell 13 件 + parts 11 件全部无需回补
  - Round 2 落地路径：在 `packages/components/src/form/input/` 下按 Figma 26206:20590 section 内的 Input 相关节点落地输入类 cell；每个 cell 配套 `.tsx` + `.types.ts` + `.contract.yaml` + `.test.tsx` + `.harness.tsx` + `index.ts`；card=true/false 仍作为同一组件内 style variant（不拆组件，冻结决议）
  - Round 2 复用策略：直接复用 Wave 1 原子（InputField / ErrorTips / ErrorText / ErrorPop / WordLimit / Label / Divider / OperationButton 的 delete/textButton 变体等）；复用 `form/exhibit/_internal/ExhibitFrame` 与 `form/exhibit/_internal/exhibitParts` 的设计模式（但如与 input 语义差异过大，在 `form/input/_internal/` 新建同构的 InputFrame 与片段，不要跨 form/exhibit 反向依赖）；如 Figma 明确要求新增输入子件（如带单位 / 选择器等），按「先补原子再装 cell」执行
  - **Round 2 cell 宽度硬约束**：在 `form/input/_internal/InputFrame` 内同样导出 `INPUT_FRAME_WIDTH_CARD` / `INPUT_FRAME_WIDTH_FLUSH` 等宽度常量，cell 外层硬编码引用并补 `flexShrink: 0 + boxSizing: border-box`；不要回到 `width: 100%` 跟随父容器拉伸的写法（已固化为治理决议）
  - **Round 2 stage page CSS 硬约束**：新建 `apps/stage/src/pages/form-input/index.css` 时，必须在文件首行写 `/*postcss-pxtransform disable*/` 注释；否则 Taro 会按 designWidth=750 把 px 二次缩放约对半（12px font-size → 实际 ~6px）；如完全通过 `var(--stage-font-size-*)` 引用 stage 全局 CSS variable 则可豁免此约定（与 tag/index.css 同模式）
  - Round 2 舞台承接：在 `apps/stage/src/pages/form-input/` 新建页，在 `componentLinks.ts` 的 `Form 表单单元` 分组中 `Exhibit 展示类` 之下、`Atoms 原子组件` 之上追加 `Input 输入类` 导航项（Atoms 仍居分组末位，冻结决议）；`app.config.ts` 同步追加 `pages/form-input/index`
  - Round 2 交互协议保持：无 hover、无默认 cursor:pointer；有切换态的 cell 保持受控 + 非受控双模；错误态通过 InputField 的 `status="error"` + ErrorTips/ErrorText/ErrorPop 原子组合，不在 cell 层硬编码错误色
  - 若 Round 2 校准视觉时发现 7 个 nestOrigin local icon 或 5 条 veriFace 装饰 SVG 偏移明显，用 Figma MCP 拉原节点 SVG 后替换对应 registry 条目；不得引入额外图标渲染管线
  - 若 Round 2 出现多点装饰图消费（当前仅 ExhibitVeriFace 单点旁路），再评估 Icon.tsx source 分支是否接入 decorative
  - Round 3（Action 类 cell）紧随 Round 2；Round 3 完成后本 exec-plan 可移至 `docs/exec-plans/completed/`
  - Button（Base 类）收尾可在 Round 2 之前/之间穿插推进，但不阻塞 Round 2 主线
  - 如未来出现明确 RN 消费方，再重新评估是否恢复更强的 RN 运行态验证
