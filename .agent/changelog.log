# AI 变更日志（追加式）

## 2026-04-06 — 取消 Phase 4 扩展：RN 运行态验证

### 代码变更
- 删除 `apps/rn-runtime`：移除本轮新增的 React Native 运行态验证载体及其 iOS/Pods/vendor 相关文件
- `packages/adapters/src/rn/button.tsx`：撤回仅为真实 React Native primitives 运行态接入而放宽的 `ComponentType<any>` 与相关宽泛类型
- `packages/adapters/src/rn/button.test.tsx`：撤回配套的 `ActivityIndicator` 宽泛类型测试改动
- `pnpm-lock.yaml`：回收 RN runtime 引入的 workspace importer 与相关锁文件变更

### 文档与治理
- 删除 `docs/exec-plans/active/phase-4-rn-runtime-validation.md`
- 新增 `docs/exec-plans/active/phase-5-await-figma-input.md`，作为取消扩展后的新的 active 入口
- `.agent/checkpoint.yaml`：将当前 focus 切回“Phase 4 主准出已完成，等待下一步目标或 Figma 输入”

### 工程验证
- 已停止本轮 `react-native run-ios` / Metro 相关进程
- 计划在清理完成后复跑 `pnpm build` 与 `pnpm check-boundaries`

### 背景
- 用户明确决定直接取消整个“Phase 4 扩展：RN 运行态验证”
- 考虑到 `apps/rn-runtime` 带来的机器负载和当前主线收益不匹配，本轮不再保留真实 RN 运行链验证载体
- Phase 4 主准出结论不受影响；未来若有明确 RN 消费方，再按需单独立项

## 2026-04-05 — Phase 4 后半段推进：小程序最小构建成功，Weex 策略补全

### 代码变更
- `apps/example-consumer/package.json`：新增 `build:weapp` 脚本，并接入 `@tarojs/plugin-platform-weapp`
- `apps/example-consumer/config/index.ts`：新增 `mini.postcss.cssModules` 配置，保持 H5 与小程序的 CSS Modules 命名策略一致
- `packages/components/package.json`：补充 `@tarojs/plugin-platform-weapp`，使设计系统组件包能够参与小程序平台解析
- `packages/adapters/src/weex/compatibility-notes.md`：由占位文档补全为可审查的 Weex 兼容桥策略评估

### 文档与治理
- `docs/exec-plans/active/phase-4-weapp-validation.md`：新增微信小程序最小构建验证文档，记录三次真实构建过程与最终成功结果
- `.agent/checkpoint.yaml`：记录“小程序最小构建已通过、运行态验证受微信开发者工具缺失阻塞”的当前状态

### 工程验证
- `pnpm --filter @fdesign/example-consumer build:weapp` 通过
- `pnpm check-boundaries` 通过
- `pnpm build` 通过
- `brew install --cask wechatwebdevtools` 通过
- `cli open --project "apps/example-consumer/dist"` 通过
- `cli islogin` 返回 `{"login":true}`

### 背景
- 为降低首次小程序验证复杂度，选择单页面的 `apps/example-consumer` 作为首个验证载体，而非页面更多的 `apps/stage` 或业务噪音更高的 `real-project-1`
- 两次失败都暴露了真实平台缺口：先缺 `@tarojs/react`，再缺 `packages/components` 的 `weapp` 平台解析能力；补齐后第三次构建成功，说明小程序路径已成立
- 当前已完成工具安装、服务端口启用与 CLI 自动打开项目；剩余最后一步是人工确认开发者工具中的页面渲染结果

## 2026-04-05 — Phase 4 第二段推进：stage 多端对照、H5 基线与小程序前置检查

### 代码变更
- `apps/stage/package.json`：新增 `@fdesign/adapters` 依赖，用于在 stage 中直接消费 RN render spec
- `apps/stage/src/pages/button/index.tsx`：新增 `Phase 4 Cross-platform Mapping` 区块，展示 Button 当前 props 到 RN adapter 的最小映射结果
- `apps/stage/src/shell/styles/layout.module.css`：新增对照卡片与键值说明样式

### 文档与治理
- `docs/exec-plans/active/phase-4-h5-webview-baseline.md`：新增 H5 / WebView 最小性能基线，记录 `real-project-1` 当前 dist 资源数量与体积
- `docs/exec-plans/active/phase-4-weapp-precheck.md`：新增微信小程序前置检查文档，固定当前仓库缺口与本机环境缺口
- `.agent/checkpoint.yaml`：当前 focus 更新为第二段进行中，并将下一步建议切到小程序构建验证与 Weex 策略补全

### 工程验证
- `pnpm install` 通过（stage 新增 `@fdesign/adapters` workspace 依赖）
- `pnpm --filter @fdesign/stage build` 通过
- `pnpm check-boundaries` 通过
- `pnpm build` 通过

### 背景
- 第一段已完成 adapters 骨架与 RN Button 最小闭环，第二段优先补齐“可审查入口”和“基线/预检查制品”，降低后续小程序与 Weex 实施时的信息缺口
- 当前仍未进入真实小程序构建或运行态验证，原因是平台插件与本机工具链尚未补齐，需按 precheck 文档逐项收敛

## 2026-04-05 — Phase 4 第一段：adapters 骨架与 RN Button 最小闭环

### 代码变更
- `packages/adapters`：新增 adapters package，包含 `package.json`、`tsconfig.json`、`eslint.config.mjs`、`vitest.config.ts`
- `packages/adapters/src/rn/button.tsx`：新增注入式 RN Button adapter 工厂与 `getReactNativeButtonRenderSpec`，将 `ButtonProps` 映射为 RN 容器/文本/加载态渲染规约
- `packages/adapters/src/rn/button.test.tsx`：新增最小验证测试，通过注入 fake primitives 验证 Button 在 RN adapter 路径上的静态渲染与交互映射
- `packages/adapters/src/weex/index.ts` 与 `packages/adapters/src/weex/compatibility-notes.md`：新增 Weex 桥接占位导出与策略文档落点
- `packages/eslint-config/rules/boundaries.js`：新增 adapters 边界规则，并禁止 `packages/components` 反向依赖 adapters
- `scripts/validate-boundaries.ts`：将 `packages/adapters` 纳入边界扫描范围，补齐对应禁依赖规则

### 工程验证
- `pnpm install` 通过（workspace 新增 `@fdesign/adapters`）
- `pnpm --filter @fdesign/adapters build` 通过
- `pnpm --filter @fdesign/adapters test` 通过
- `pnpm --filter @fdesign/adapters lint` 通过
- `pnpm check-boundaries` 通过
- `pnpm build` 通过

### 文档与治理
- `.agent/checkpoint.yaml`：记录 Phase 4 第一段完成状态，并将下一步建议切换到多端对照、性能基线、小程序验证与 Weex 策略补全

### 背景
- Phase 4 第一段目标是先证明 adapters 分层与 RN Button 最小闭环成立，不在仓库平台依赖和本机移动端环境未齐备时贸然扩大 scope
- 当前仓库仍只有 H5 平台插件，本机也缺少 Java Runtime、watchman、CocoaPods、adb 等若干移动端工具，因此本轮优先完成“可构建、可验证、可治理”的最小路径

## 2026-04-05 — Phase 3 补充验收：独立拆出验证完成

### 代码变更
- `scripts/verify-standalone-project.ts`：新增独立拆出验证脚本，将 `real-project-1` 与其 design system 依赖复制到仓库外临时目录，改写依赖与 tsconfig 后执行真实 `install / build`
- `package.json`：新增 `verify-standalone-project` 根脚本

### 文档与治理
- `docs/exec-plans/completed/phase-3-standalone-verification.md`：新增 Phase 3 补充验收执行文档，明确更严格的“独立拆出运行”验证口径
- `.agent/checkpoint.yaml`：记录 `phase-3-standalone-extraction-validation` 已完成

### 工程验证
- `pnpm verify-standalone-project` 通过
- 仓库外 standalone 目录中的 `pnpm install` 通过
- 仓库外 standalone 目录中的 `pnpm build` 通过
- 浏览器实际打开 standalone `dist/`，页面正确渲染为 `Campaign Launch Console / 活动发布台`
- `.deliverables/standalone-verification/real-project-1/verification-report.md` 已生成

### 背景
- 项目总目标已要求真实业务项目可独立拆出、独立构建、独立交付，但此前 Phase 3 的显式验收更偏重 monorepo 内独立构建与交付
- 本次补充验收用于将“拎出仓库后仍可 install/build/run”落成真实验证，降低治理口径和实际能力不一致的风险

## 2026-04-05 — 治理写入：Phase 4 执行计划激活

### 文档与治理
- `docs/exec-plans/active/phase-4.md`：新增 Phase 4 执行计划，聚焦 `packages/adapters`、RN adapter、Button 首个跨端验证、H5 / WebView 性能基线、小程序构建验证与 Weex 兼容桥评估
- `.agent/checkpoint.yaml`：`active_exec_plan` 切换到 `docs/exec-plans/active/phase-4.md`
- `.agent/checkpoint.yaml`：`current_focus` 更新为“Phase 4 执行计划已产出，准备进入多端适配实施”

### 背景
- Phase 3 已完成且验收通过，但治理层尚未生成 Phase 4 活动执行计划，导致当前阶段缺少统一实施入口
- 本次写入用于完成治理层切换，为下一轮 Phase 4 实施提供明确的步骤和边界

## 2026-04-05 — Phase 3: 真实业务交付闭环完成

### 代码变更
- `projects/real-project-1`：新增真实业务样板项目，包含 `package.json`、`tsconfig.json`、`babel.config.js`、`config/`、`src/`、`project.yaml`
- `projects/real-project-1/src/pages/index/index.tsx`：实现最小“Campaign Launch Console”业务页，组合 `Button / Input / Card / ListItem / Tag / Text / Modal`
- `projects/real-project-1/manual-acceptance.md`：新增人工验收步骤文档
- `scripts/build-deliverable.ts`：新增交付构建脚本，读取 `project.yaml` 并输出 `.deliverables/real-project-1/`
- `scripts/validate-deliverable.ts`：新增交付校验脚本，验证 `dist/`、`assets-manifest.json`、`deploy-readme.md`、`smoke-report.md`
- `package.json`：新增 `build-deliverable` 与 `validate-deliverable` 根脚本

### 工程验证
- `pnpm install` 通过（接入新增 workspace 项目）
- `pnpm --filter @fdesign/real-project-1 build` 通过
- `pnpm build-deliverable` 通过
- `pnpm validate-deliverable` 通过
- `pnpm check-boundaries` 通过
- `pnpm build` 通过
- `real-project-1` H5 构建存在 entrypoint size warning（app 约 300 KiB），当前作为性能观察项记录，未阻塞 Phase 3 闭环

### 文档与治理
- `docs/ROADMAP.md`：Phase 3 状态更新为已完成
- `.agent/checkpoint.yaml`：切换到 Phase 4 焦点并记录 Phase 3 完成状态
- `docs/exec-plans/completed/phase-3.md`：归档 Phase 3 执行计划

### 背景
- Phase 3 的核心目标是证明 `projects/*` 可以在不依赖 `apps/stage` 的前提下，独立消费设计系统并生成可交付产物
- 本轮优先做单项目最小闭环，避免过早抽象成通用发布平台

## 2026-04-05 — 治理补完：Phase 2 归档与 Phase 3 激活

### 文档与治理
- `docs/exec-plans/completed/phase-2.md`：归档 Phase 2 执行计划
- `docs/exec-plans/active/phase-3.md`：新增 Phase 3 执行计划，聚焦 `real-project-1` 交付闭环
- `.agent/checkpoint.yaml`：`active_exec_plan` 切换到 `docs/exec-plans/active/phase-3.md`

### 背景
- Phase 2 代码与准出标准已完成，但 `exec-plans` 目录仍停留在 `phase-2` 活动计划，未完成治理层阶段切换
- 本次补完用于确保“当前阶段执行计划”与 `ROADMAP` / `checkpoint` 的阶段状态保持一致

## 2026-04-05 — Phase 2: 组件集扩展 + 消费层验证完成

### 代码变更
- root：新增 `yaml` 与 `@changesets/cli` 依赖，补充 `validate-contracts` / `changeset` / `version-packages` / `release` 脚本
- packages/contracts：新增 contracts package，提供 `ComponentContract` 类型与最小 shape 校验
- scripts：新增 `validate-contracts.ts`，扫描 `packages/components/src/**/*.contract.yaml` 并交叉校验对应 `*.types.ts`
- packages/components：新增 7 个高频组件目录并接入根导出
  - `text/`
  - `tag/`
  - `input/`
  - `card/`
  - `list-item/`
  - `icon/`
  - `modal/`
- packages/components：每个新增组件均包含 `*.tsx / *.types.ts / *.contract.yaml / *.module.css / *.test.tsx / *.harness.tsx / index.ts`
- apps/stage：扩展首页导航与组件导航，新增 `text / tag / input / card / list-item / icon / modal` 展示页
- apps/example-consumer：新增独立 Taro H5 consumer 样板，验证只通过 `@fdesign/components` 消费组件
- `.changeset/`：初始化 changesets 版本管理配置
- `.github/workflows/ci.yml`：新增 `pnpm validate-contracts` 校验步骤

### 工程验证
- `pnpm validate-contracts` 通过（8 个合同文件）
- `pnpm --filter @fdesign/components test` 通过（8 个测试文件，15 个用例）
- `pnpm --filter @fdesign/components build` 通过
- `pnpm --filter @fdesign/stage build` 通过
- `pnpm --filter @fdesign/example-consumer build` 通过
- `pnpm check-boundaries` 通过

### 文档与治理
- `docs/ROADMAP.md`：Phase 2 状态改为已完成，并勾选准出标准
- `.agent/checkpoint.yaml`：切换到 Phase 3 焦点，记录 Phase 2 完成状态与下一步建议

### 背景
- Phase 2 的核心目标是把 Phase 1 验证出的单组件模式扩展为可消费的最小组件集，并证明 consumer 不依赖 stage 也能正常使用设计系统
- 本轮优先做最小合同校验与消费链路验证，避免过早进入完整 schema 工程或把 example-consumer 做成第二个 stage

## 2026-04-04 — Phase 1 全链路模式验证完成

### 代码变更
- root：补充 Vitest 基础依赖与 `.github/workflows/ci.yml` 最小 CI
- packages/components：引入 Taro 4.1.11、Vitest、React 18 基础依赖，`test` 切换到可执行 Vitest
- packages/components：新增 `Phase1Probe`，用于最小编译验证入口
- packages/components：落地 Button 全套标准结构
  - `Button.tsx`
  - `Button.types.ts`
  - `Button.contract.yaml`
  - `Button.module.css`
  - `Button.test.tsx`
  - `Button.harness.tsx`
  - `button/index.ts`
- packages/components：构建脚本追加 CSS 资源复制，保证 `dist` 可被 stage 消费
- apps/stage：新增 Taro H5 stage 应用（config / app / pages / shell）
- scripts：新增 `copy-css-assets.mjs`，同步 `*.module.css` 到构建产物

### 工程验证
- `pnpm build` 通过（包含 `@fdesign/stage`）
- `pnpm lint` 通过
- `pnpm test` 通过（Button 2 个用例）
- `pnpm check-boundaries` 通过
- `apps/stage` 构建成功
- `apps/stage` 开发服务启动成功，监听 `http://localhost:10086/`

### 文档与治理
- `docs/ROADMAP.md`：Phase 1 状态改为已完成，准出标准全部勾选
- `.agent/checkpoint.yaml`：切换到 Phase 2 焦点
- `.agent/decisions.log`：补充 Phase 1 实施期运行时决策
- `docs/exec-plans/active/`：准备切换到 Phase 2 执行计划

### 背景
- 在真实实施中验证了 Taro 4.1.11 + pnpm monorepo + H5 stage 的完整链路
- 通过显式补齐 `@tarojs/shared`、Babel / React refresh 依赖，以及 `dist` 样式复制，解决了 monorepo 下的 H5 运行时约束

## 2026-04-03 — Phase 0: 仓库骨架

### 新增
- monorepo 根配置：package.json、pnpm-workspace.yaml、turbo.json、tsconfig.base.json
- AGENTS.md 导航文件（~85行）
- docs/ARCHITECTURE.md 架构地图
- .agent/ 状态管理（checkpoint.yaml、decisions.log、changelog.log）
- docs/exec-plans/active/phase-0-repo-scaffold.md 执行计划
- packages/tokens/ — 设计 token package（含初始 provisional 值）
- packages/components/ — 组件 package 空骨架
- packages/eslint-config/ — ESLint 配置 + 边界约束规则
- packages/tsconfig/ — 共享 TypeScript 配置
- scripts/validate-boundaries.ts — import 边界校验脚本
- .gitignore、.npmrc

### 决策
- 见 .agent/decisions.log 2026-04-03 条目

## 2026-04-03 — 治理体系补全：北极星 + 路线图

### 新增
- docs/NORTH_STAR.md — 项目定位精华（~50行），不可变核心
- docs/ROADMAP.md — 全 Phase 路线图（含准入/准出标准）

### 变更
- AGENTS.md — 阅读协议升级为四层记忆架构

### 背景
- 识别到 Agent 跨对话无预定义路线图会导致方向漂移
- 解决方案：ROADMAP.md 预定义全部 Phase + NORTH_STAR.md 锚定不可变定位

## 2026-04-04 — 红队审查改进措施落地

### 代码变更
- packages/tokens：ESLint 接入（eslint.config.mjs + 依赖 + lint 脚本）
- packages/components：ESLint 接入（eslint.config.mjs + 依赖 + lint 脚本 + boundaries 规则）
- scripts/validate-boundaries.ts：补齐 consumer→stageShell 规则，扫描范围增加 apps/ 目录

### 文档变更
- docs/exec-plans/active/phase-1.md：增加步骤 0（ESLint 接入）、步骤 1 增加 Vitest 引入和最小编译验证、风险部分增加 CSS Modules 多端策略声明
- docs/ROADMAP.md：Phase 2 准出标准增加 changeset、Phase 3 增加 scope 限定说明（验证样板，非外部交付）
- docs/ARCHITECTURE.md：Package 职责表增加"创建阶段"列
- .agent/decisions.log：补充 Storybook 否决原因、CSS Modules 多端策略、adapter 注入扩展点策略
- .agent/checkpoint.yaml：标记红队审查改进完成

### 背景
- 架构红队审查发现：ESLint 边界约束未实际生效（lint 是 echo 占位）、validate-boundaries.ts 规则与 boundaries.js 不一致、测试基础设施缺失、多端策略未显式声明、Phase 3 scope 未限定等问题
- 原架构设计师确认改进措施后，本轮执行全部 P0-P3 改进

## 2026-04-05 — H5 / 小程序预览错位修复

### 代码变更
- scripts/copy-css-assets.mjs：复制 components CSS 到 dist 时同步移除 `:global(...)` 包装，保证消费端直接加载发布产物时选择器仍有效
- apps/example-consumer/package.json：将 H5 / weapp 输出目录拆分为 `dist-h5` 与 `dist-weapp`
- apps/example-consumer/config/index.ts：`outputRoot` 改为读取 `TARO_OUTPUT_ROOT`

### 文档变更
- docs/exec-plans/active/phase-4-weapp-validation.md：补充运行态预览错位的两个根因、修复方式与新的 H5 / weapp 验收入口
- .agent/checkpoint.yaml：更新为“等待人工确认 dist-weapp 页面渲染”的当前状态

### 验证
- 执行 `pnpm --filter @fdesign/components build`
- 执行 `pnpm --filter @fdesign/example-consumer build`
- 执行 `pnpm --filter @fdesign/example-consumer build:weapp`
- 浏览器通道确认 `http://127.0.0.1:10091` 的 example-consumer H5 页面正常渲染，无明显样式缺失
- 微信开发者工具 CLI 成功重新打开 `apps/example-consumer/dist-weapp`

### 背景
- 之前出现的 H5 “纯文本退化”不是页面结构缺失，而是发布态 CSS 仍保留原始 CSS Modules `:global(...)` 语法，消费端未再处理导致组件选择器失效
- 之前小程序打开成欢迎页，与 H5 / weapp 共用 `dist` 目录导致产物被另一端覆盖有关；拆分输出目录后，预览入口已稳定

## 2026-04-05 — Phase 4 正式收口

### 文档变更
- docs/ROADMAP.md：将 Phase 4 状态更新为“已完成”
- docs/exec-plans/completed/phase-4.md：归档 Phase 4 主执行计划
- docs/exec-plans/active/phase-4-weapp-validation.md：将微信小程序验证状态从“等待人工确认”更新为“运行态验收通过”
- .agent/checkpoint.yaml：更新为 Phase 4 已完成状态，并切换归档后的执行计划路径

### 验证
- 用户已人工确认 H5 与微信小程序两端均正常渲染
- 执行 `pnpm build`
- 执行 `pnpm check-boundaries`

### 背景
- Phase 4 准出项已全部满足：`packages/adapters` 骨架、RN Button 最小闭环、stage 多端对照、H5 / WebView 基线、微信小程序运行态验收，以及 Weex 兼容桥策略文档均已完成

## 2026-04-05 — Phase 4 收口后目录清理

### 文档变更
- `docs/exec-plans/active/phase-4-h5-webview-baseline.md` → `docs/exec-plans/completed/phase-4-h5-webview-baseline.md`
- `docs/exec-plans/active/phase-4-weapp-precheck.md` → `docs/exec-plans/completed/phase-4-weapp-precheck.md`
- `docs/exec-plans/active/phase-4-weapp-validation.md` → `docs/exec-plans/completed/phase-4-weapp-validation.md`
- `.agent/checkpoint.yaml`：移除残留的 `active/phase-4-*` 路径引用，整理为与“Phase 4 已完成”一致的当前态快照
- `.gitignore`：补充 `dist-h5/` 与 `dist-weapp/`，避免双端构建产物误入版本控制

### 背景
- `docs/exec-plans/active/` 语义应仅承载当前未完成阶段的执行计划；Phase 4 已完成后，辅助基线与验证文档继续留在 `active/` 会误导后续 Agent
- `phase-4-weapp-precheck.md` 补充“历史快照”说明，避免其中的旧环境判断与当前仓库状态混淆

## 2026-04-05 — Phase 4 下一步 active 计划补齐

### 文档变更
- `docs/exec-plans/active/phase-4-rn-runtime-validation.md`：新增 Phase 4 后续扩展计划，聚焦真实 RN 运行态验证
- `.agent/checkpoint.yaml`：将 `active_exec_plan` 切换到新的 RN 运行态验证计划，并同步更新当前 focus 与 next recommended

### 背景
- 按项目惯例，收口后需要在 `docs/exec-plans/active/` 中补出“下一步执行计划”
- 当前用户已明确下一步决策为“继续 Phase 4 扩展：RN 运行态验证”，因此补出新的 active 计划而不直接跳到 Phase 5

## 2026-04-03 — 语言统一 + Phase 1 执行计划

### 变更
- 所有治理文档统一为中文（AGENTS.md、ARCHITECTURE.md、ROADMAP.md、changelog.log、decisions.log）
- Phase 0 执行计划移至 completed/

### 新增
- docs/exec-plans/active/phase-1.md — Phase 1 详细执行计划

### 背景
- 项目人类负责人为中文母语者，中英混杂增加审查成本
- 代码、变量名、package name 保持英文（行业标准）
- 治理文档统一中文，提升可审查性

## 2026-04-06 — Phase 5 token 协议抽取与映射方案落档

### 文档变更
- `docs/exec-plans/active/phase-5-token-protocol-and-mapping.md`：新增 Phase 5 token 协议抽取与仓库映射方案
- `.agent/checkpoint.yaml`：将当前阶段切换为 `phase-5`，并将 active plan 指向新的 token 协议文档

### 背景
- 用户已提供 `/Users/gavinss/Desktop/GUI-playgournd/GUIbySpec/vars.css`，并补齐了 spacing、radius、typography 等缺失项与使用备注
- 本轮输入已超出“token 值”本身，包含了 radius、spacing、font-weight、line-height 等协议层约束
- 为避免后续新对话丢失这些规则，先将协议与映射方案固化到仓库文档，待用户确认后再进入仓库 token 实施

## 2026-04-06 — Phase 5 协议确认补记

### 文档变更
- `docs/exec-plans/active/phase-5-token-protocol-and-mapping.md`：补充 `Card / Modal` 的“外层容器 + 内层内容行”两级 padding 协议，并记录 `Text.weight` 收缩 API 的确认结论
- `.agent/checkpoint.yaml`：同步记录上述确认已完成

### 背景
- 用户明确选择 `Text.weight` 采用“收缩 API”而非兼容 remap
- 用户认可 `Card / Modal` 不采用单一 padding 值，而是按外层容器与内层内容行分层建模

## 2026-04-06 — Phase 5 内容行节奏歧义修正

### 文档变更
- `docs/exec-plans/active/phase-5-token-protocol-and-mapping.md`：将“内层内容行 padding”修正为“内容行间距 / 内容行节奏”，并补充首行/末行不得与外层容器 padding 机械叠加的规则
- `.agent/checkpoint.yaml`：同步记录该歧义修正

### 背景
- 用户补充说明：`18 / 14 / 12 / 14` 更接近内容行与相邻行元素之间的间距定义，而非统一作用在内容行自身上下的 padding
- 若不显式记录，后续实施时容易把外层容器 padding 与首行 top padding 叠加，造成顶部留白过大

## 2026-04-06 — Phase 5 Button Figma 对齐

### 代码变更
- `packages/components/src/button/*`：按 Figma 节点重构 Button API、尺寸、状态与样式，实现 `primary-fill / primary-outline / secondary-outline`、`xl / l / m / s / xs / mini` 与 `inactive`
- `packages/adapters/src/rn/button.tsx` 与 `button.test.tsx`：同步 RN adapter 映射到新的 Button 规格
- `apps/stage/src/pages/button/index.tsx` 与 `apps/stage/src/shell/PropControl.tsx`：同步 Stage 的 Button 演示页与控制面板到真实 Figma 规格
- `packages/components/src/modal/Modal.tsx`、`apps/example-consumer/src/pages/index/index.tsx`、`projects/real-project-1/src/pages/index/index.tsx`：更新 Button 消费用法到新的变体命名
- `packages/components/src/button/Button.contract.yaml`：Button 从 `provisional: true` 切为 `provisional: false`

### 文档变更
- `docs/exec-plans/active/phase-5-token-protocol-and-mapping.md`：标记 Button 已完成首个 Figma 组件对齐
- `.agent/checkpoint.yaml`：同步记录 token 收口落地与 Button 已完成

### 验证
- `pnpm build`：通过
- `pnpm validate-contracts`：通过
- `pnpm check-boundaries`：通过
- `pnpm --filter @fdesign/adapters test -- --run src/rn/button.test.tsx`：通过
- `pnpm test` / `pnpm --filter @fdesign/components test ...`：仍受既有 `@tarojs/runtime` 环境问题 `ENABLE_INNER_HTML is not defined` 影响

## 2026-04-10 — Stage 壳层重构首轮

### 代码变更
- `apps/stage/src/app.config.ts`：移除首页入口，默认从 `Button` 页进入，并同步窗口标题为 `FenQiLe Components Preview`
- `apps/stage/src/app.css`、`apps/stage/src/shell/styles/layout.module.css`：建立舞台层私有黑白灰样式基线，改为固定顶栏、左侧可滚动导航、右侧独立展示区，并统一移除舞台壳层圆角与过渡效果
- `apps/stage/src/shell/Layout.tsx`、`componentLinks.ts`、`PropControl.tsx`：导航改为“英文 / 中文”，增加当前页高亮，按钮控制面板同步切为双语标签
- `apps/stage/src/pages/button/index.tsx`：重构为 Hero / Playground / Gallery / Verification 四段结构，把 RN 映射和 harness 下沉到底部辅助验证区
- `apps/stage/src/pages/index/index.tsx`：删除旧首页页面；其它组件页同步切换为双语标题

### 验证
- `pnpm --filter @fdesign/stage build`：通过
- `ReadLints`：本轮改动文件未发现新增 linter 问题
- 当前仍存在既有打包体积告警：`app` 入口 309 KiB 超过 webpack 推荐阈值，但不影响本轮功能落地

## 2026-04-11 — Stage 文字样式 token 收口

### 代码变更
- `apps/stage/src/app.css`：新增舞台页私有 typography token（header/nav/component title/section title 的字号与行高）
- `apps/stage/src/shell/styles/layout.module.css`：将 `__stage-headerTitle`、`__stage-navLink`、`__stage-title`、`__stage-heroTitle`、`__stage-sectionTitle` 切换为引用私有 token
- `apps/stage/src/shell/styles/layout.module.css`：移除会覆盖上述字号的响应式字体重写，确保全场景统一遵循新规格

### 验证
- `ReadLints`：本轮改动文件未发现新增 linter 问题

## 2026-04-11 — Stage 描述文案字号收敛

### 代码变更
- `apps/stage/src/app.css`：新增舞台私有 token `--stage-font-size-description: 12PX` 与 `--stage-line-height-description: 18PX`
- `apps/stage/src/shell/styles/layout.module.css`：`__stage-description` 与 `__stage-caption` 统一引用上述 token

### 验证
- `ReadLints`：本轮改动文件未发现新增 linter 问题

## 2026-04-11 — Stage 切页残余抖动清零

### 代码变更
- `apps/stage/src/shell/Layout.tsx`：导航切页从 `redirectTo` 进一步收敛为 `reLaunch`，避免首次命中页面时的短程过渡抖动
- `apps/stage/src/app.css`：仅针对 Taro 路由容器层（`.taro_router` / `.taro_page`）强制禁用 `animation/transition/transform`
- 保持禁用范围仅在路由壳层，不影响展示区组件内部动效（如 Button loading）

### 验证
- `ReadLints`：本轮改动文件未发现新增 linter 问题

## 2026-04-11 — Stage 导航跳页无转场修正

### 代码变更
- `apps/stage/src/shell/Layout.tsx`：导航切页从 `Taro.navigateTo` 改为 `Taro.redirectTo`，避免入栈页面转场动画
- `apps/stage/src/shell/Layout.tsx`：增加失败兜底 `Taro.reLaunch`，保证跳页稳定

### 验证
- `ReadLints`：本轮改动文件未发现新增 linter 问题

## 2026-04-11 — Stage 侧栏滚轮穿透兜底修正

### 代码变更
- `apps/stage/src/app.css`：补充 `#app` 与 `#app > div` 锁高与禁滚，避免路由首层容器参与页面滚动
- `apps/stage/src/shell/Layout.tsx`：在 `__stage-sidebar` 增加 `onWheel` 拦截（`preventDefault + stopPropagation`），阻止侧栏滚轮事件穿透到展示区或整页

### 验证
- `ReadLints`：本轮改动文件未发现新增 linter 问题

## 2026-04-11 — Stage 滚动容器隔离修正

### 代码变更
- `apps/stage/src/app.css`：将 `html/body/#app/page` 高度统一为 `100%`，并对 `body/page` 设置 `overflow: hidden`，禁止整页滚动
- `apps/stage/src/shell/styles/layout.module.css`：
  - `__stage-layout` 改为固定 `height: 100vh` 并 `overflow: hidden`
  - `__stage-sidebar`、`__stage-main` 添加 `overscroll-behavior: contain`，阻断滚动链路向页面冒泡

### 验证
- `ReadLints`：本轮改动文件未发现新增 linter 问题

## 2026-04-11 — Stage 侧栏宽度改为内容自适应

### 代码变更
- `apps/stage/src/shell/styles/layout.module.css`：
  - `__stage-layout` 改为 `flex` 布局，主区与侧栏进入同一流式布局
  - `__stage-sidebar` 从固定宽度改为 `width/min-width: max-content`，由最长导航项决定宽度
  - `__stage-main` 移除固定 `left` 偏移，改为 `flex: 1` 跟随侧栏自适应
  - 移除断点里基于固定侧栏宽度的覆盖逻辑

### 验证
- `ReadLints`：本轮改动文件未发现新增 linter 问题

## 2026-04-11 — Stage 导航宽度按最长文案收敛

### 代码变更
- `apps/stage/src/shell/styles/layout.module.css`：
  - `__stage-navList` 改为 `inline-flex` + `width/min-width: max-content`，使导航列表宽度由最长文案决定
  - `__stage-navLink` 增加 `width: 100%`，确保所有导航项统一跟随最长项宽度
  - `__stage-navLink` 增加 `white-space: nowrap`，避免换行导致宽度计算不稳定
- `__stage-navLink` 仍保持 `padding: var(--stage-space-nav-link-padding)`，当前固定为 `16PX`

### 验证
- `ReadLints`：本轮改动文件未发现新增 linter 问题

## 2026-04-11 — Stage 文案密度与导航节奏二次收敛

### 代码变更
- `apps/stage/src/app.css`：新增舞台私有 token，覆盖导航内边距与本轮指定字号（heroLabel、heroDescription、metaKey、metaValue、controlLabel、toggle、subsectionTitle）
- `apps/stage/src/shell/styles/layout.module.css`：
  - 导航项内边距改为统一 `--stage-space-nav-link-padding`（固定 16PX，移除断点差异）
  - 头部背景从 `--stage-surface` 改为 `--stage-page-bg`，与页面/侧栏保持一致
  - 指定文本类改为引用舞台私有 token，并保持 `PX` 物理像素
- `apps/stage/src/pages/button/index.tsx`：移除 Gallery 卡片头部右侧重复的尺寸 `hint` 文案
- `apps/stage/src/shell/styles/layout.module.css`：删除已不再使用的 `__stage-galleryCardHint` 样式

### 验证
- `ReadLints`：本轮改动文件未发现新增 linter 问题

## 2026-04-11 — Stage 头部内边距与高度修正

### 代码变更
- `apps/stage/src/shell/styles/layout.module.css`：`__stage-header` 调整为四边一致内边距 `16PX`
- `apps/stage/src/app.css`：`--stage-header-height` 从 `176PX` 收敛到 `84PX`，避免头部上下留白过大
- `apps/stage/src/shell/styles/layout.module.css`：小屏断点下 `sidebar/main` 的 `top` 改为复用 `--stage-header-height`，保持壳层一致

### 验证
- `ReadLints`：本轮改动文件未发现新增 linter 问题

## 2026-04-11 — Stage 导航项内边距对齐修正

### 代码变更
- `apps/stage/src/shell/styles/layout.module.css`：将 `__stage-navLink` 内边距改为四边一致，默认 `32PX`
- `apps/stage/src/shell/styles/layout.module.css`：在断点下同步改为 `24PX` 与 `16PX`，分别对应主区 `padding`，建立左对齐关系并保持上下右内边距一致

### 验证
- `ReadLints`：本轮改动文件未发现新增 linter 问题

## 2026-04-11 — Stage 侧栏宽度下调 20%

### 代码变更
- `apps/stage/src/app.css`：`--stage-sidebar-width` 从 `360PX` 调整为 `288PX`
- `apps/stage/src/shell/styles/layout.module.css`：断点侧栏宽度与主区 `left` 同步按 20% 下调（`280PX -> 224PX`，`220PX -> 176PX`）

### 验证
- `ReadLints`：本轮改动文件未发现新增 linter 问题

## 2026-04-11 — Stage 侧栏宽度适配修正

### 代码变更
- `apps/stage/src/app.css`：将 `--stage-header-height`、`--stage-sidebar-width` 改为 `PX`，避免壳层关键尺寸被 Taro px transform 缩放
- `apps/stage/src/shell/styles/layout.module.css`：将 `@media` 下侧栏宽度与主区 `left` 偏移改为 `PX`，保证导航变宽后布局仍对齐

### 验证
- `ReadLints`：本轮改动文件未发现新增 linter 问题

## 2026-04-11 — Stage 字号缩放修正

### 代码变更
- `apps/stage/config/index.ts` 排查确认：H5 使用 `designWidth: 750`，小写 `px` 会参与 Taro 自适应换算
- `apps/stage/src/app.css`：将舞台私有字号 token 与对应行高从 `px` 改为 `PX`，避免被 px transform 缩放

### 验证
- `ReadLints`：本轮改动文件未发现新增 linter 问题

## 2026-04-11 — Button 舞台宽度与 outline 状态修正

### 代码变更
- `apps/stage/src/shell/styles/layout.module.css`：为 `__stage-stack` 增加 `align-items: flex-start`，避免 Gallery 中按钮被列容器横向拉伸
- `packages/components/src/button/Button.tsx`：补充宽度变量（`--button-width/min-width/max-width`），并将 `l` 尺寸约束到 `min 178px / max min(327px, calc(100vw - 48px))`
- `packages/components/src/button/Button.tsx`：修正 `fd-button-loadingWithSpinner` 仅在 `loading && spinner.show` 时生效，避免默认态误挂 loading 专用 class
- `packages/components/src/button/Button.tsx` 与 `packages/adapters/src/rn/button.tsx`：修正 `primary-outline` 默认/loading 的前景色来源，RN 同步 `l` 尺寸宽度区间约束
- `packages/components/src/button/Button.module.css`：引入宽度变量消费，并补充 `primary-outline` 文案色兜底规则

### 验证
- `ReadLints`：本轮改动文件未发现新增 linter 问题
- 本地舞台页（`http://localhost:10086/#/pages/button/index`）复看：按钮不再在 Gallery 列容器内自动撑满

## 2026-04-11 — Button XL/L 尺寸与 loading 素材对齐 GUIbySpec

### 代码变更
- `packages/components/src/button/Button.tsx`：按 GUIbySpec 将 `xl/l` 固定宽度改为 `355px/327px`，并保持按钮文案字重 `500`
- `packages/components/src/button/Button.module.css`：spinner 改为使用 SVG 资源渲染并保留旋转动效
- `packages/components/src/button/assets/loading-on-primary-true.svg`、`packages/components/src/button/assets/loading-on-primary-false.svg`：新增 loading 素材
- `packages/adapters/src/rn/button.tsx`：RN 映射同步 `xl/l` 宽度 `355/327`
- `scripts/copy-css-assets.mjs`：扩展构建脚本，除 CSS 外同步复制组件静态资源到 `dist`，修复 stage 对 `dist` 消费时的 SVG 丢失

### 验证
- `pnpm --filter @fdesign/components build`
- `pnpm --filter @fdesign/adapters build`
- `ReadLints`：本轮改动文件未发现新增 linter 问题

## 2026-04-11 — Stage 全站移除 Playground/辅助验证模块

### 代码变更
- `apps/stage/src/pages/button/index.tsx`：移除 `Playground / 实时预览` 模块
- `apps/stage/src/pages/button/index.tsx`：移除 `Verification / 辅助验证` 模块
- `apps/stage/src/pages/button/index.tsx`：同步清理相关状态、映射变量与无用导入，页面结构收敛为 Hero + Gallery

### 验证
- `ReadLints`：`apps/stage/src/pages/button/index.tsx` 未发现新增 linter 问题

## 2026-04-11 — Button 总览重组为四块收纳

### 代码变更
- `apps/stage/src/pages/button/index.tsx`：Gallery 改为「类型 / 尺寸 / 状态矩阵 / 不同尺寸 Loading」四块结构
- `apps/stage/src/pages/button/index.tsx`：类型区使用 `L + default` 展示三种类型，并在按钮下使用灰色 `__stage-metaKey` 标注类型名称
- `apps/stage/src/pages/button/index.tsx`：尺寸区使用 `primary-fill + default` 展示全尺寸，并在按钮下使用灰色 `__stage-metaKey` 标注尺寸名称
- `apps/stage/src/pages/button/index.tsx`：状态矩阵区改为按类型分卡片，每卡使用 `L` 尺寸并在按钮左侧以灰色文案标注 `default/inactive/disabled/loading`
- `apps/stage/src/pages/button/index.tsx`：loading 对照区改为 `primary-outline` 下全尺寸 loading 展示，并在按钮下使用灰色 `__stage-metaKey` 标注尺寸名称
- `apps/stage/src/shell/styles/layout.module.css`：新增总览行、矩阵网格与矩阵行样式，适配上述四块布局

### 验证
- `ReadLints`：改动文件无新增 linter 问题
- 本地预览：`http://localhost:10086/#/pages/button/index?stamp=matrix` 结构已按四块收纳呈现

## 2026-04-11 — Button 矩阵布局与文案/Loading 细节修正

### 代码变更
- `apps/stage/src/pages/button/index.tsx`：状态矩阵行内按钮包裹层改为独立容器，便于两端布局与统一按钮宽度
- `apps/stage/src/shell/styles/layout.module.css`：状态矩阵行改为 `space-between`，按钮列固定宽度并统一撑满，保证各状态按钮等宽
- `packages/components/src/button/Button.tsx`：收敛 `m/s/xs` 文案字号与行高（`m/s=12/14`，`xs=10/11`），并更新 `m/s/xs/mini` 的 loading spinner 显示规则与尺寸
- `packages/adapters/src/rn/button.tsx`：同步 `m/xs` 文案字号行高与 `m/s/xs/mini` loading spinner 显示规则

### 验证
- `pnpm --filter @fdesign/components build`
- `pnpm --filter @fdesign/adapters build`
- `ReadLints`：本轮改动文件未发现新增 linter 问题
- 本地预览：`http://localhost:10086/#/pages/button/index?stamp=matrix2` 已看到矩阵两端布局、等宽按钮与 M-mini loading 展示生效

## 2026-04-11 — Button 文案统一与小尺寸 Loading 宽度保持

### 代码变更
- `apps/stage/src/pages/button/index.tsx`：舞台页按钮文案统一为“操作文本”
- `packages/components/src/button/Button.tsx`：新增小尺寸 loading 隐文案规则（`m/s/xs/mini`），以隐藏文案占位保持宽度并仅显示 spinner
- `packages/components/src/button/Button.module.css`：新增 `fd-button-loadingHideLabel`、`fd-button-spinnerAnchor`、`fd-button-labelGhost` 样式，支撑“无可见文案但宽度不变”
- `apps/stage/src/pages/button/index.tsx` + `apps/stage/src/shell/styles/layout.module.css`：状态矩阵按钮容器固定列宽并撑满，确保按 default 基准等宽

### 验证
- `pnpm --filter @fdesign/components build`
- `ReadLints`：改动文件无新增 linter 问题
- 本地预览：`http://localhost:10086/#/pages/button/index?stamp=optext` 已看到文案统一、矩阵等宽、M-mini loading 无可见文案

## 2026-04-11 — Stage 文案真实 PX 对齐

### 代码变更
- `apps/stage/src/shell/styles/layout.module.css`：将 stage 内残留裸写小写 `px` 的文本样式改为复用舞台私有字号 token，避免再次走 Taro `px` transform 缩放
- `apps/stage/src/shell/styles/layout.module.css`：`__stage-galleryCardLabel` 改为复用 `__stage-description` 同链路的 `12PX/18PX`，使矩阵卡片标题与说明文案按真实像素等大
- `apps/stage/src/shell/styles/layout.module.css`：`__stage-kvKey`、`__stage-kvValue` 同步改为复用 `meta` 字号 token，保证后续凡标注为多少 `px` 都按真实物理像素理解

### 验证
- `ReadLints`：`apps/stage/src/shell/styles/layout.module.css` 未发现新增 linter 问题

## 2026-04-11 — 组件样式 token 治理约束补记

### 治理变更
- `.agent/decisions.log`：追加用户确认的新约束：新增组件样式值只能来自本项目全局 token；设计稿硬编码值必须先映射到仓库 token，禁止直接落到组件实现
- `.agent/checkpoint.yaml`：同步记录上述治理约束，供后续 Agent 接续执行

## 2026-04-11 — 组件样式 token 治理约束修正

### 治理变更
- `.agent/decisions.log`：将“必须只用全局 token”的规则修正为“优先使用全局 token；若当前 token 暂无对应项，需先询问用户确认，才允许临时使用硬编码或 reference token”
- `.agent/decisions.log`：补充过渡态闭环要求：当前实现任务结束前，必须把临时值做语义抽取，落到本项目全局 token，再回灌替换组件实现
- `.agent/checkpoint.yaml`：同步记录上述修正规则，供后续 Agent 按同一口径执行

## 2026-04-11 — Button token 收口与回灌

### 代码变更
- `packages/tokens/src/colors.ts`：为 `semantic.action.primary` 新增 `outlineForeground`、`inactiveBackground`、`inactiveBorder`、`inactiveForeground`，收口 Button 对 `reference brand blue` 的直接消费
- `packages/tokens/src/spacing.ts`：新增 `spacing.semantic.borderWidthHairline`，并为 `spacing.component.button` 扩展 `xl / l / m / s / xs / mini` 六档高度、宽度、内部 `paddingX` 与 `spinnerSize`
- `packages/tokens/src/motion.ts` + `packages/tokens/src/index.ts`：新增并导出 `motion / duration / easing / distance`，承接 Button transition、spinner rotation 与 press offset
- `packages/tokens/src/web-vars.css`：同步补齐上述颜色、border width、Button 尺寸与 motion 的 Web CSS variables 镜像
- `packages/components/src/button/Button.tsx`：Button H5 实现改为消费新全局 token；移除直接 `reference token` 与尺寸硬编码；`xl/l` 在 `block` 场景下切到 `paddingXFluidMin`
- `packages/components/src/button/Button.module.css`：transition、spinner size、active 位移全部改为消费 token 映射值，不再直接写死 `120ms / 1000ms / 1px / 20px`
- `packages/adapters/src/rn/button.tsx`：RN adapter 同步改为消费新全局 token；颜色、hairline 边框与六档尺寸规格对齐 H5，并修正 `block` 场景被固定宽度覆盖的问题

### 验证
- `ReadLints`：`packages/tokens/src`、`packages/components/src/button`、`packages/adapters/src/rn/button.tsx` 无新增 linter 问题
- `pnpm --filter @fdesign/tokens build && pnpm --filter @fdesign/components build && pnpm --filter @fdesign/adapters build`
- 本地 stage dev server 已重新编译成功，Button 页 DOM 内容存在；浏览器侧仍观察到页面内容被头部区域遮挡，属于后续需单独排查的 stage 布局问题

## 2026-04-11 — Stage Button 页文案收敛

### 代码变更
- `apps/stage/src/pages/button/index.tsx`：移除 Hero 顶部 `Component Preview` 标签，避免主卡片首屏出现多余英文辅助信息
- `apps/stage/src/pages/button/index.tsx`：移除 `Gallery / 变体总览` 模块描述文案，使总览区直接进入设计对照内容
- `apps/stage/src/pages/button/index.tsx`：将 Hero 主描述改为“按钮用于执行用户在交互流程中触发指令、提交更改或完成的即时操作。”

## 2026-04-11 — Stage 展示区纵向间距分级

### 代码变更
- `apps/stage/src/shell/styles/layout.module.css`：提升 `__stage-main`、`__stage-demoCard`、`__stage-demoBody` 的纵向间距，拉开页面级与卡片级内容分组
- `apps/stage/src/shell/styles/layout.module.css`：将 `__stage-heroBody`、`__stage-galleryStack`、`__stage-gallerySection`、`__stage-gallerySection + .__stage-gallerySection` 的间距改为更清晰的层级节奏，增强 Button 页分区辨识度
- `apps/stage/src/shell/styles/layout.module.css`：同步放宽 `__stage-heroMeta`、`__stage-galleryCard`、`__stage-overviewRow`、`__stage-matrixGrid`、`__stage-matrixStack` 的纵向留白，使信息块更透气，但未修改 Button 组件自身内部 spacing

### 验证
- `ReadLints`：`apps/stage/src/shell/styles/layout.module.css` 未发现新增 linter 问题

## 2026-04-11 — Stage spacing token 方案 A

### 代码变更
- `apps/stage/src/app.css`：为 Button 页当前确认过的展示区纵向节奏新增 stage 私有 spacing token，token 值使用小写 `px`，保留现有 `pxTransform` 链路
- `apps/stage/src/shell/styles/layout.module.css`：将展示区相关间距改为消费上述 spacing token，保持当前手调后的视觉效果不变

### 说明
- 本次不采用大写 `PX`。原因是 stage 当前展示区 spacing 以用户手调后的 `pxTransform` 效果为验收基准；若直接切到 `PX`，同数字会按真实物理像素渲染，视觉会整体变大

### 验证
- `ReadLints`：`apps/stage/src/app.css`、`apps/stage/src/shell/styles/layout.module.css` 未发现新增 linter 问题

## 2026-04-11 — Stage 壳层切换为 Web 原生 px 语义

### 代码变更
- `apps/stage/src/app.css`：添加 `/*postcss-pxtransform disable*/`，并将 stage 私有尺寸/字号/间距 token 的单位统一收敛为原生小写 `px`
- `apps/stage/src/shell/styles/layout.module.css`：添加 `/*postcss-pxtransform disable*/`，并将残留 `PX` 收敛为原生小写 `px`
- 保持 Button 页当前展示区 spacing token 数值不变；在当前桌面 stage 预览基准下，不再依赖 `pxTransform` 也能延续现有视觉大小

### 验证
- `ReadLints`：`apps/stage/src/app.css`、`apps/stage/src/shell/styles/layout.module.css` 未发现新增 linter 问题
- `pnpm --filter @fdesign/stage build`
- 构建产物 `apps/stage/dist/css/app.css` 已确认 stage 壳层变量与布局样式输出为原生 `px`，未再被编译为 `rem`

## 2026-04-11 — Button 状态矩阵横向收敛

### 代码变更
- `apps/stage/src/app.css`：新增 stage 私有 token `--stage-space-matrix-row-gap-min`，用于约束状态矩阵行内最小间距
- `apps/stage/src/shell/styles/layout.module.css`：将 `__stage-matrixGrid` 的列宽下限从 `420px` 收窄到 `320px`，使状态矩阵在当前桌面展示宽度下优先三列横排
- `apps/stage/src/shell/styles/layout.module.css`：将 `__stage-matrixRow` 的 `gap` 改为消费 `--stage-space-matrix-row-gap-min`，在保留两端自适应分布的同时保证子项最小间距为 `24px`

### 验证
- `ReadLints`：`apps/stage/src/app.css`、`apps/stage/src/shell/styles/layout.module.css` 未发现新增 linter 问题
- `pnpm --filter @fdesign/stage build`
- 构建通过；仅保留既有 `EntrypointsOverSizeLimitWarning`，与本次样式调整无关

## 2026-04-11 — Stage 主展示区 padding 固定化

### 代码变更
- `apps/stage/src/shell/styles/layout.module.css`：移除 `__stage-main` 在 `1024px` 与 `720px` 断点下的 padding 覆盖，主展示区统一保持默认 `var(--stage-space-main-padding)`

### 验证
- `ReadLints`：`apps/stage/src/shell/styles/layout.module.css` 未发现新增 linter 问题

## 2026-04-12 — 治理层 Phase 5 跟踪准度校正

### 治理变更
- `docs/ROADMAP.md`：将 Phase 5 从“未开始”校正为“进行中”，并明确其为按需插入、长期滚动推进的工作面
- `.agent/checkpoint.yaml`：收缩为当前事实快照，移除过长流水账，统一到“当前 focus / 当前约束 / next recommended”口径
- `docs/exec-plans/active/phase-5-token-protocol-and-mapping.md`：升级为 Phase 5 唯一长期滚动主计划，补充当前事实快照、当前工作窗口、滚动 backlog 管理方式与完成判定
- `docs/exec-plans/completed/phase-5-await-figma-input.md`：将旧等待态文档移出 `active/`，改为历史归档记录，避免与当前主计划并列造成歧义

### 说明
- 本轮未改动组件实现代码；目标仅为校正治理层对实际工程进度的跟踪精度
- 当前 Phase 5 的统一事实源为：`docs/ROADMAP.md` + `.agent/checkpoint.yaml` + `docs/exec-plans/active/phase-5-token-protocol-and-mapping.md`

## 2026-04-12 — completed 执行计划归档口径统一

### 治理变更
- `docs/exec-plans/completed/phase-3.md`：补充“Phase 3 主执行计划”归档角色说明，并指向独立拆出验证补充文档
- `docs/exec-plans/completed/phase-3-standalone-verification.md`：补充“Phase 3 补充验收记录”定位，并回链主计划
- `docs/exec-plans/completed/phase-4.md`：补充“Phase 4 主执行计划”归档角色说明，并列出相关补充基线/验证文档
- `docs/exec-plans/completed/phase-4-h5-webview-baseline.md`、`phase-4-weapp-precheck.md`、`phase-4-weapp-validation.md`：统一补充归档角色与主计划回链
- `docs/exec-plans/completed/phase-5-await-figma-input.md`：补充“Phase 5 历史等待态记录”归档角色说明

### 说明
- 本轮不做大规模文件重命名，避免制造新的路径漂移
- 归档层统一策略改为：保留既有文件名，优先通过文档头部明确“主计划 / 补充验证 / 基线 / 历史等待态”的角色分工

## 2026-04-12 — Phase 5 状态术语收敛

### 治理变更
- `docs/ROADMAP.md`：为 Phase 5 补充状态词口径说明，并将当前状态表述收敛到“进行中 / 已完成”这一阶段层术语
- `.agent/checkpoint.yaml`：将当前 focus、事实快照与 next recommended 的措辞统一到“已落地首轮 / 收尾中 / 可冻结”
- `docs/exec-plans/active/phase-5-token-protocol-and-mapping.md`：新增“状态词口径”小节，并将 `Button`、token 与当前工作窗口的表述统一到同一套组件批次术语

### 说明
- 本轮只统一当前事实源中的状态术语，不回写历史文档与历史 changelog 条目
- 当前约定为：阶段层使用“进行中 / 已完成”；组件批次层使用“已落地首轮 / 收尾中 / 可冻结”

## 2026-04-12 — Phase 5 状态词口径决策入库

### 治理变更
- `.agent/decisions.log`：追加“Phase 5 状态词口径统一”决策，明确阶段层与组件批次层的状态词边界，并指定 `ROADMAP / checkpoint / active plan` 的职责分工

### 说明
- 本轮是治理收尾动作，不涉及代码实现调整
- 至此，Phase 5 的状态词约定已同时落到事实源文档与决策层文档

## 2026-04-13 — stage 侧栏箭头对齐 Figma

### 代码变更
- `apps/stage/src/shell/styles/layout.module.css`：将 stage 侧栏分组导航箭头从 border 拼接形状替换为基于 Figma 节点 `25860:3434` 的 chevron SVG mask，实现与设计稿一致的 24x24 图标轮廓，同时保留展开/收起方向切换

### 说明
- 本轮未调整导航结构与交互逻辑，仅校正 `__stage-navArrowWrap` 容器内的图标内容与视觉表现
- 保持现有 `Button` 收尾窗口不变，本次属于 stage 展示校正的一部分

## 2026-04-13 — stage 可点击热区光标优化

### 代码变更
- `apps/stage/src/shell/styles/layout.module.css`：将 stage 点击光标逻辑收敛为通用规则，默认覆盖侧栏导航与控制面板内的可点击项，并新增 `__stage-clickable` 作为未来非导航自定义点击区的显式复用入口

### 说明
- 组件本体默认不纳入这组 cursor 规则，避免把演示区内业务组件交互样式一起改写
- 后续 stage 新增非导航点击热区时，可直接复用 `__stage-clickable`，不再继续散落单点补 `cursor: pointer`

## 2026-04-13 — 一级折叠导航点击规则优化

### 代码变更
- `apps/stage/src/shell/Layout.tsx`：为一级分组导航补充显式点击规则，折叠态点击时仅在该分组当前没有已选中二级项时才跳转到首个二级导航；若该分组已有选中项，则只展开；展开态点击时仍仅折叠分组而不切换当前二级导航

### 说明
- 本轮未改动二级导航项自身点击逻辑；二级项仍按原有规则直接切换页面
- 该行为用于让一级折叠导航在“入口点击”和“收起操作”之间保持稳定一致的交互预期

## 2026-04-13 — 一级导航展开状态跨页面保留

### 代码变更
- `apps/stage/src/shell/Layout.tsx`：将一级分组展开状态持久化到 stage 本地存储，并在壳层初始化时恢复；因此点击非折叠一级导航进入新页面后，先前已展开的折叠分组不会因 `Layout` 重建被自动收起

### 说明
- 初始化时仅在本地尚无该分组记录时，才默认展开当前激活分组，避免覆盖用户已经手动折叠/展开过的状态
- 本轮不改动已有一级/二级点击语义，只补齐跨页面状态记忆

## 2026-04-13 — 可折叠一级导航着色收敛

### 代码变更
- `apps/stage/src/shell/Layout.tsx`：移除可折叠一级导航对 `__stage-navLinkActive` 的依赖，使其在展开态和折叠态下都保持未展开态的默认颜色；二级导航选中态保持不变

### 说明
- 本轮仅调整可折叠一级导航本身的着色，不改动 hover 反馈与二级导航的选中态样式
- 展开态仍通过箭头方向和二级列表显隐表达状态，不再额外用一级项激活色强调

## 2026-04-13 — stage 组件命名双语格式统一

### 代码变更
- `apps/stage/src/shell/componentLinks.ts`：统一导航与页面标题中的组件命名格式，将 `English / 中文` 收敛为 `English 中文`
- `apps/stage/src/pages/button/index.tsx`：同步调整 Button 页标题、Hero 标题与变体命名文案
- `apps/stage/src/pages/button-flush/index.tsx`、`button-griditem/index.tsx`、`button-text-button/index.tsx`、`button-fab/index.tsx`：同步调整占位页中的组件命名文案
- `apps/stage/src/pages/text/index.tsx`、`input/index.tsx`、`tag/index.tsx`、`card/index.tsx`、`list-item/index.tsx`、`icon/index.tsx`、`modal/index.tsx`：同步调整页面级组件标题
- `apps/stage/src/pages/button/index.config.ts`、`button-flush/index.config.ts`、`button-griditem/index.config.ts`、`button-text-button/index.config.ts`、`button-fab/index.config.ts`：同步调整页面导航栏标题

### 说明
- 本轮只调整“组件命名”相关双语展示，不改动 `Coming Soon / 待补充`、`Variant / 类型` 等通用说明性文案
- 当前 stage 中组件命名相关文案已统一为“英文名 + 一个空格 + 中文名”的格式

## 2026-04-13 — stage 页面内容结构按 Base 样板统一

### 代码变更
- 新增 `apps/stage/src/shell/StageShowcasePage.tsx`：抽出仅负责内容区的统一样板，复用 `Base 基础按钮` 页的 `Hero + Gallery 分节` 结构与既有 stage 样式类
- `apps/stage/src/pages/text/index.tsx`、`input/index.tsx`、`tag/index.tsx`、`card/index.tsx`、`list-item/index.tsx`、`icon/index.tsx`、`modal/index.tsx`：改为统一样板结构，保留各组件现有示例内容并重排为英雄区、统计信息和分节展示
- `apps/stage/src/pages/button-flush/index.tsx`、`button-griditem/index.tsx`、`button-text-button/index.tsx`、`button-fab/index.tsx`：改为与 `Base 基础按钮` 同结构的占位页，先补齐 Hero、元信息和分节占位内容
- `apps/stage/src/pages/text/index.config.ts`、`input/index.config.ts`、`tag/index.config.ts`、`card/index.config.ts`、`icon/index.config.ts`、`list-item/index.config.ts`、`modal/index.config.ts`：同步将导航栏标题改为与页面一致的双语标题

### 说明
- `apps/stage/src/pages/button/index.tsx` 未改动，继续作为所有 stage 组件页的内容样板
- 本轮未改动 `apps/stage/src/shell/Layout.tsx`、`componentLinks.ts` 或 stage 壳层样式，因此 header、主导航栏和展示区整体布局保持不变
- 已执行 `pnpm --filter @fdesign/stage build`，构建通过；仅保留既有的入口体积 warning，未引入新的构建错误

## 2026-04-13 — stage 移除矩阵验证模块

### 代码变更
- `apps/stage/src/pages/text/index.tsx`、`input/index.tsx`、`tag/index.tsx`、`card/index.tsx`、`icon/index.tsx`、`list-item/index.tsx`、`modal/index.tsx`：删除 `矩阵验证 / Harness Matrix` 分节，并移除对应的 `*Harness` 组件导入

### 说明
- 本轮遵循 `Base 基础按钮` 页的展示取向：舞台页只保留面向设计和交互预览的内容，不再承载代码层矩阵验证
- `Base 基础按钮` 页未改动，stage 壳层布局也未改动
- 已确认 stage 源码中不再残留 `Harness Matrix` 相关文案，且本轮修改无新增 lint 问题

## 2026-04-14 — Text 组件下线并重构为 Typography 基础页

### 代码变更
- `apps/stage/src/shell/componentLinks.ts`、`apps/stage/src/app.config.ts`：新增一级折叠导航 `CssToken 全局样式` 并置于最前，将 `Typography 排版` 作为其下唯一二级页；移除原顶层 `Text 文本` 导航
- 新增 `apps/stage/src/pages/typography/index.tsx`、`index.config.ts`：以字体族、字号、字重、行高和基础文本行为重建排版验证页，不再展示 `Text` 组件 props 矩阵
- 删除 `apps/stage/src/pages/text/index.tsx`、`index.config.ts`：移除旧的 `Text 文本` 页面实现
- `packages/components/src/index.ts`、`packages/components/src/text/*`：移除 `Text` 组件、类型、合同、测试与 harness 出口
- `apps/example-consumer/src/pages/index/index.tsx`、`projects/real-project-1/src/pages/index/index.tsx`：将原先依赖 `@fdesign/components/Text` 的文案替换为原生文本元素 + 页面层样式/局部 token 样式写法
- `apps/example-consumer/src/app.css`、`projects/real-project-1/src/app.css`：补齐页面层文本样式类，承接 `Text` 组件下线后的文案展示
- `docs/ROADMAP.md`、`docs/exec-plans/active/phase-5-token-protocol-and-mapping.md`、`.agent/checkpoint.yaml`、`.agent/decisions.log`：同步治理层口径，明确 `Text` 不再作为设计系统组件保留，排版验证改由 `Typography` 基础页承担

### 说明
- 本轮确认“文字展示由全局 typography token 与页面/组件语义样式组合完成”，不再围绕通用 `Text` 组件扩展 API
- 已执行 `pnpm --filter @fdesign/components build && pnpm --filter @fdesign/stage build && pnpm --filter @fdesign/example-consumer build && pnpm --filter @fdesign/real-project-1 build`，全部通过；仅保留既有的 H5 entry size warning

## 2026-04-14 — components 测试基座恢复可用

### 代码变更
- `packages/components/vitest.config.ts`：新增 `setupFiles`，为组件测试统一注入运行前预置逻辑
- 新增 `packages/components/vitest.setup.ts`：补齐 `ENABLE_INNER_HTML` 全局常量，并统一 mock `@tarojs/taro` 的最小能力，避免 Vitest 在 jsdom 下直接拉起真实 Taro 运行时

### 说明
- 本轮未修改任何组件生产实现，只修复 `packages/components` 的测试环境隔离问题
- 已执行 `pnpm --filter @fdesign/components test`，7 个测试文件、14 个测试全部通过

## 2026-04-14 — Icon 双源架构与首轮展示落地

### 代码变更
- `packages/tokens/src/spacing.ts`：新增 `spacing.component.icon`，沉淀 `special-mini / xxs / xs / s / m / special-large` 六档 icon 容器尺寸与 padding 协议
- `packages/components/src/icon/Icon.tsx`、`Icon.types.ts`、`Icon.module.css`、`Icon.contract.yaml`、`Icon.test.tsx`、`Icon.harness.tsx`：将旧字符型 placeholder 重构为统一 `Icon` 入口，支持 `local / material` 双源、六档尺寸、tone/color 覆盖与无障碍属性
- 新增 `packages/components/src/icon/iconRegistry.tsx`、`LocalIconRenderer.tsx`、`MaterialIcon.tsx`：落地 local inline SVG 注册表、Material Symbols 渲染器，以及自动分发兼容层
- `apps/stage/src/pages/icon/index.tsx`、`apps/stage/src/shell/componentLinks.ts`：重写 Icon 展示页为 Hero + Local / Material / Sizes / Tones 分节，并将导航位置移到 Button 之后
- `apps/stage/src/app.css`、`apps/example-consumer/src/app.css`、`projects/real-project-1/src/app.css`：引入 Material Symbols Rounded 字体，保证 H5 路径下 material source 与旧 icon 名兼容可用

### 说明
- 本轮先稳定落地双源架构与首轮本地图标集；受当前 Figma MCP 返回远程资产 URL 而非稳定可入库 path 数据限制，local registry 先以内联 SVG 首轮集起步，并保留后续继续批量补齐的扩展位
- 旧调用中的 `info / check / chevron-right` 无需立即改代码即可继续工作；未显式传 `source` 时，组件默认按“本地注册表优先，否则 material”分发
- 已执行 `pnpm --filter @fdesign/tokens build && pnpm --filter @fdesign/components test && pnpm --filter @fdesign/components build && pnpm --filter @fdesign/stage build && pnpm --filter @fdesign/example-consumer build && pnpm --filter @fdesign/real-project-1 build`，全部通过；仅保留既有的 H5 entry size warning

## 2026-04-14 — 本地图标集扩充到用户导出的 40 个 SVG

### 代码变更
- `packages/components/src/icon/iconRegistry.tsx`：用用户提供的 `/Users/gavinss/Downloads/Icons` 导出结果替换首轮示例图标，完整接入 40 个本地 SVG，并按 kebab-case 名称统一收口为 local registry
- `packages/components/src/icon/LocalIconRenderer.tsx`：改为基于 registry 中的 `path d + fillRule/clipRule` 通用渲染，不再只依赖少量手写示例组件
- `apps/stage/src/pages/icon/index.tsx`：将 local 图标展示扩充为完整 40 图标预览，Hero 元信息同步更新

### 说明
- 本轮使用的是用户本地导出的正式 SVG 文件，不再依赖临时 Figma 远程资产 URL 作为本地图标源
- 现有 `Icon` 双源架构不变，仅将 local source 从“首轮子集”提升为“完整 40 图标集”
- 已再次执行 `pnpm --filter @fdesign/components test && pnpm --filter @fdesign/components build && pnpm --filter @fdesign/stage build && pnpm --filter @fdesign/example-consumer build && pnpm --filter @fdesign/real-project-1 build`，全部通过；仅保留既有的 H5 entry size warning

## 2026-04-14 — Icon local 图标补充语义别名层

### 代码变更
- `packages/components/src/icon/iconRegistry.tsx`：新增 `LOCAL_ICON_ALIASES`、`resolveLocalIconName()` 与 `hasLocalIcon()`，为部分文件名风格的 local icon 补充语义别名，同时保留旧名兼容
- `packages/components/src/icon/Icon.tsx`：自动分发逻辑改为识别 alias 后的 local icon，未显式传 `source` 时新旧命名都能正确路由到 local source
- `apps/stage/src/pages/icon/index.tsx`：local 图标展示切换为“语义别名优先，旧 registry 名对照展示”的口径
- `packages/components/src/icon/Icon.test.tsx`、`Icon.contract.yaml`：补充 alias 自动路由回归测试与命名约束说明

### 说明
- 本轮采用“非破坏式整理”：优先新增语义别名，而不是直接删除旧文件名派生名称，避免影响现有消费方
- 当前已整理的重点是明显偏资源文件名风格的命名，如 `credit-card / cards-1 / automatic-deduction / we-chat` 等；其余名称后续仍可继续按业务语义微调
- 已执行 `pnpm --filter @fdesign/components test && pnpm --filter @fdesign/components build && pnpm --filter @fdesign/stage build`，全部通过；仅保留既有的 H5 entry size warning

## 2026-04-14 — Icon local 图标补充第二轮业务语义别名

### 代码变更
- `packages/components/src/icon/iconRegistry.tsx`：继续补充 `amount-increase / encryption / billing-reminder / repayment-history / housing-fund / document-details / red-envelope-cashout` 等第二轮 local alias
- `apps/stage/src/pages/icon/index.tsx`：将对应图标展示名称切换到第二轮业务语义别名，并继续保留 legacy registry 名对照

### 说明
- 本轮仍保持“新增 alias、旧名不删”的兼容策略，避免影响已经依赖旧 registry 名的页面
- 第二轮主要收敛账单、还款、加额、公积金、红包提现等业务场景名称，使展示与后续调用更接近业务语义，而不是资源导出名
- 已执行 `pnpm --filter @fdesign/components test && pnpm --filter @fdesign/components build && pnpm --filter @fdesign/stage build`，全部通过；仅保留既有的 H5 entry size warning

## 2026-04-14 — stage Icon 展示区排版优化

### 代码变更
- `apps/stage/src/pages/icon/index.tsx`：新增 `toDisplayLabel()` 与 `IconGalleryItem`，将 local/material 展示区收口为统一 tile 渲染；local 区不再并排展示 `alias + legacyName`，仅显示单个 display name
- `apps/stage/src/app.css`：新增 `__stage-iconTile*` 样式，为 icon 展示项提供统一宽度、固定内边距、居中预览与多词换行标签布局

### 说明
- 本轮仅针对 `Icon` 页展示区做局部排版优化，未改动全局 `__stage-captionedItem`，因此不会影响 Button、Tag、Typography 等其他展示页
- 当前 tile 规则为：top/left/right `24px`、bottom `12px`，并让 bottom padding 与 icon-label gap 保持一致
- 名称展示统一转成空格分词形式，例如 `bank-card` -> `bank card`、`shopping_cart_checkout` -> `shopping cart checkout`，超长名称可按词换行
- 已执行 `pnpm --filter @fdesign/stage build`，构建通过；仅保留既有 H5 entry size warning

## 2026-04-14 — stage Gallery 头部与 Icon 页细节继续收敛

### 代码变更
- `apps/stage/src/shell/ComponentDemo.tsx`：移除 demo 卡片头部的描述文本渲染，`Gallery / 组件总览` 下方说明行不再出现在任何页面
- `apps/stage/src/pages/icon/index.tsx`：`Tones` 分节改为仅展示 `info` 图标；`Sizes` 分节为 local/material 两列补充尺寸展示名映射，页面中改显示 `mini / large`
- `apps/stage/src/app.css`：为 `Sizes` 分节预览容器补充 `rgb(255 61 111 / 30%)` 底色

### 说明
- 本轮“尺寸名称更改”当前仅体现在 stage 展示层文案，未改动 `Icon` 组件运行时 API 与 token 协议，避免引入不必要的兼容性风险
- 已执行 `pnpm --filter @fdesign/stage build`，构建通过；仅保留既有 H5 entry size warning

## 2026-04-14 — stage 新增备注类小组件并接入 Icon 页

### 代码变更
- 新增 `apps/stage/src/shell/StageNoteText.tsx`：提供仅供 stage 展示层使用的备注文字组件，默认文案为“备注文案”
- `apps/stage/src/pages/icon/index.tsx`：在 `Material / info` 尺寸卡标题旁接入 `StageNoteText`，当前展示“外部组件绘图区不受控”
- `apps/stage/src/app.css`：新增 `__stage-noteText` 样式，使用灰色 `10px` 备注文案口径

### 说明
- 该组件仅用于 stage 展示层备注，不进入 `packages/components`，也不参与设计系统组件合同
- 已执行 `pnpm --filter @fdesign/stage build`，构建通过；仅保留既有 H5 entry size warning

## 2026-04-14 — stage header 接入品牌 logo

### 代码变更
- 新增 `apps/stage/src/assets/fd-logo.png`：复制用户提供的 logo 资源到 stage 内部静态资源目录，避免直接引用下载目录文件
- `apps/stage/src/shell/Layout.tsx`：在 header 标题左侧新增品牌 logo 容器
- `apps/stage/src/shell/styles/layout.module.css`：新增 `__stage-headerBrand` 与 `__stage-headerLogo` 样式，logo 使用 `background-image` 引用本地资源，并将尺寸绑定到 `--stage-font-size-header-title`

### 说明
- 用户提供的原图为 `168 x 168`，当前按方形资源等比缩放，不做额外裁切或拉伸
- 已执行 `pnpm --filter @fdesign/stage build`，构建通过；仅保留既有 H5 entry size warning

## 2026-04-14 — stage 浏览器页签补齐 favicon 与站点名

### 代码变更
- `apps/stage/src/index.html`：新增 `<link rel="icon">`，使用现有 `fd-logo.png` 的 base64 data URL 作为浏览器 favicon
- `apps/stage/src/index.html`：将页签标题统一为 `FDesign System`

### 说明
- 本轮仅修改 H5 入口模板，不影响 stage 壳层、组件运行时逻辑和静态资源输出结构
- favicon 采用内联 data URL，避免新增 public 目录或额外 copy 配置，降低路径失配风险
- 已执行 `pnpm --filter @fdesign/stage build`，构建通过；仅保留既有 H5 entry size warning

## 2026-04-14 — stage 一级折叠导航补充展开激活背景态

### 代码变更
- `apps/stage/src/shell/Layout.tsx`：为一级折叠导航新增 `isExpandedGroupActive` 判定，仅在“一级已展开且其下存在当前选中二级项”时挂载 `__stage-navLinkGroupActive`
- `apps/stage/src/shell/styles/layout.module.css`：新增 `.__stage-navLinkGroupActive`，背景色先复用二级导航选中态的 `var(--stage-surface)`

### 说明
- 本轮只补一级折叠导航的背景激活态，不复用 `__stage-navLinkActive`，因此不会给一级项带上左侧 active 边框
- 已执行 `ReadLints` 与 `pnpm --filter @fdesign/stage build`，均通过；仅保留既有 H5 entry size warning

## 2026-04-15 — stage 扩建 CssToken 展示页

### 代码变更
- `apps/stage/src/app.config.ts`：将 CssToken 页统一迁移到 `pages/css-token/*` 路由下，并新增 `Color / Spacing / Radii` 页面入口
- `apps/stage/src/shell/componentLinks.ts`：补齐 `CssToken 全局样式` 分组下的 `Typography / Color / Spacing / Radii` 四个二级导航
- `apps/stage/src/pages/css-token/typography/index.tsx`：将既有 `Typography` 页迁移到新目录，保留 Hero + Gallery 展示结构
- `apps/stage/src/pages/css-token/color/index.tsx`：新增颜色展示页，完整展开 `referenceColors` 与 `semanticColors` 叶子节点，并展示色块、token 路径、实际值与已存在的 CSS var 镜像
- `apps/stage/src/pages/css-token/spacing/index.tsx`：新增间距展示页，拆分为 `Scale / Semantic / Component Horizontal / Component Vertical / Component Metrics` 五个分节；component 间距改为使用示意矩形与标注矩形表达，不再借用真实组件
- `apps/stage/src/pages/css-token/radii/index.tsx`：新增圆角展示页，展示四档圆角值及统一矩形示意
- `apps/stage/src/pages/css-token/shared.tsx`：新增 css-token 共享辅助，统一处理 token 叶子节点展开、路径文案、CSS var 映射与基础预览卡片
- `apps/stage/src/app.css`：新增 stage 私有示意 token `--stage-preview-element` 与 `--stage-annotation`，并补充颜色块、圆角块、尺寸示意、间距标注矩形等展示样式
- 删除 `apps/stage/src/pages/typography/index.tsx`：旧路径已收敛到 `pages/css-token/typography/index.tsx`

### 说明
- 本轮明确不纳入 `Motion` 与 `Shadow`；当前 `CssToken` 仅覆盖已确认并要求展示的 `Typography / Color / Spacing / Radii`
- `Spacing` 页中 `component` 的尺寸类指标（如 `minHeight / width / spinnerSize / box`）单独归入 `Component Metrics`，避免将尺寸值误展示为相邻间距
- 已执行 `ReadLints` 与 `pnpm --filter @fdesign/stage build`，均通过；仅保留既有 H5 entry size warning

## 2026-04-15 — stage 调整 CssToken 导航顺序与颜色分层展示

### 代码变更
- `apps/stage/src/shell/componentLinks.ts`：交换 `CssToken 全局样式` 分组下 `Radii` 与 `Spacing` 的导航顺序
- `apps/stage/src/app.config.ts`：同步调整 css-token 页面注册顺序，保持与导航一致
- `apps/stage/src/pages/css-token/color/index.tsx`：将颜色页从按层平铺改为按嵌套结构递归分组展示；当前能按 `reference -> brand -> blue`、`semantic -> text -> primary` 等层级明确分栏
- `apps/stage/src/app.css`：新增颜色分组树的展示样式，包括分组标题、分组栈与层级缩进

### 说明
- 本轮仅调整导航顺序和颜色页展示结构，不改动 token 源数据
- 已执行 `ReadLints` 与 `pnpm --filter @fdesign/stage build`，均通过；仅保留既有 H5 entry size warning

