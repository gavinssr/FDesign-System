# 架构决策日志（追加式）

## 2026-04-03 — 初始架构方案对齐

**背景**: 项目从零开始，需要确定 Agent 治理模式、目录结构、Stage 技术选型。

**决策**:
1. 采用 OpenAI Harness Engineering "map, not manual" 模式 — AGENTS.md ~100行做导航，不做大手册
2. 目录结构扁平化，按需生长，不预创建空目录
3. Stage 层使用 Taro H5 应用（非 shadcn/ui），避免样式体系冲突
4. TypeScript 类型作为主合同，YAML 合同为补充元数据
5. Harness = 机械化 linter + 结构化测试 + CI 脚本，不建独立验证平台
6. 构建产物输出到 .deliverables/（gitignore），不入版本控制
7. Monorepo 工具链：pnpm workspaces + turborepo
8. Agent 记忆通过 .agent/checkpoint.yaml 接续，非每轮读全量文档

**被否决的替代方案**:
- 大手册模式（AGENT_PROTOCOL.md 等 6 份文档）— 消耗上下文，违反 Harness 原则
- shadcn/ui 做 Stage 外壳 — 样式冲突风险，Taro 运行时兼容问题
- 独立 harness 验证平台 — 初期维护成本过高
- 先治理后开发 — 延迟价值产出

## 2026-04-03 — 四层记忆架构 + 方向漂移防护

**背景**: Agent 跨对话记忆接续存在方向漂移风险。原有 checkpoint.yaml 只记录"当前在哪"，不记录"要去哪"。后续 Phase 的目标如果依赖 Agent 推理，会逐渐偏离项目定位。

**决策**:
1. 新增 docs/NORTH_STAR.md — 不可变的项目核心定位（~50行），除非用户显式要求否则不改写
2. 新增 docs/ROADMAP.md — 预定义全部 Phase 的准入/准出标准（~100行），Agent 每轮必读
3. Agent 每轮入口阅读量 = AGENTS.md(~85行) + checkpoint.yaml(~30行) + ROADMAP.md(~100行) ≈ 215行
4. Phase 准出标准是检查清单，Agent 完成 Phase 时逐条确认
5. Phase 不可跳过——上一 Phase 准出未达标，不可进入下一 Phase
6. Phase 5（Figma 对接）是例外，可在任何阶段插入

**被否决的替代方案**:
- 依赖聊天历史传递方向 — 上下文窗口有限，无法保证传递完整
- 依赖 Agent 从代码推理后续 Phase — 累积偏移不可控
- 把全部需求文档塞入每轮上下文 — 消耗过大（500+ 行）

## 2026-04-04 — 红队审查改进

**背景**: 架构红队审查发现多处设计缺口，原架构设计师确认后产出改进措施清单。本条目记录改进过程中的关键技术决策。

**决策**:

1. **Storybook 被否决**：Stage 层选择自建 Taro H5 应用而非 Storybook，原因：
   - Storybook 原生运行在 React DOM 环境，Taro 组件依赖 `@tarojs/runtime` 无法在非 Taro 环境正确渲染
   - Taro 4.x 对第三方 UI 库兼容性存在已知问题（GitHub issue #17078）
   - 自建 stage 即 Taro H5 应用，组件在完全原生的 Taro 运行时中渲染，零兼容风险
   - 自建 stage 的控制面板功能（Layout / ComponentDemo / PropControl）规模可控，不构成过度工程

2. **CSS Modules 多端策略（有意识的技术债）**：
   - CSS Modules 为当前 H5 + 小程序样式方案，Taro 会将其编译为 wxss
   - RN 端不支持 CSS Modules，需要 StyleSheet.create 或类似方案
   - Phase 4 多端适配时需引入 JS-in-CSS 方案或 adapter 层转换
   - Phase 1-3 建立的 `*.module.css` 模式在 Phase 4 可能需要适配调整
   - 标记为有意识的技术债——在 H5 + 小程序主路径上 CSS Modules 是正确选择，不应因 RN 未来需求而在 Phase 1 引入不必要的抽象

3. **adapter 注入扩展点策略**：
   - Phase 1 组件暂不预留 adapter 注入点
   - Phase 4 时通过 context 或 props 注入平台特定行为（如 RN 的 TouchableOpacity 替代 click 事件）
   - 理由：过早抽象会增加 Phase 1-3 的复杂度，且在未明确各端具体差异之前无法设计合理的注入 API
   - 当前防护措施：Button.contract.yaml 已标注平台差异说明，Props 命名采用 `onPress`（而非 `onClick`）面向 RN 兼容

**被否决的替代方案**:
- 使用 Storybook for Taro — 兼容性风险过高，需要额外的 Taro runtime 适配层
- Phase 1 即引入跨端样式抽象（如 styled-components 或 token-to-style 方案） — 增加不必要的复杂度，且无法在无 RN 验证环境时做出正确的抽象设计
- Phase 1 预留 adapter 注入点 — 过早抽象，缺乏足够的多端差异信息来设计合理 API

## 2026-04-04 — Phase 1 实施期运行时决策

**背景**: 在 Phase 1 实施过程中，Taro 4.1.11 + pnpm monorepo + H5 stage 的真实编译链路暴露出若干运行时约束，这些约束不是前期文档能完全预判的，需要在实施期补充为显式决策。

**决策**:
1. **apps/stage 显式依赖 `@tarojs/shared`**：
   - 原因：`@tarojs/webpack5-runner` 的 H5 基础配置会在 app 目录基于 `resolveSync()` 注入 `@tarojs/shared` alias
   - 在 pnpm monorepo 下，如果 stage 仅通过传递依赖获得 `@tarojs/shared`，该 alias 可能解析失败并导致 webpack schema 校验报错
   - 因此 `apps/stage/package.json` 显式添加 `@tarojs/shared: 4.1.11`，作为 H5 构建链路的稳定依赖

2. **apps/stage 显式提供 Babel 运行前置依赖**：
   - `babel-preset-taro`、`@babel/preset-react`、`@pmmmwh/react-refresh-webpack-plugin`、`react-refresh` 在 monorepo app 下需要显式安装
   - 原因：Taro H5 app 的 Babel/React refresh 链路不会可靠地从其他 workspace 包隐式继承
   - 目标：确保 `apps/stage` 的 build/dev 都能独立成立，不依赖“根目录恰好已有某依赖”

3. **Phase 1 中 `*.module.css` 采用“side-effect import + 显式命名空间 class”消费策略**：
   - 真实表现：Taro H5 会正确产出 CSS，但在当前 monorepo 组合下，`import styles from '*.module.css'` 未得到稳定的 JS 导出对象
   - 组件层采用 `fd-button-*` 前缀，stage shell 采用 `__stage-*` 前缀，继续保留样式隔离目标
   - 这不是放弃 CSS Modules 文件模式，而是在 Phase 1 用更稳的消费方式换取运行时确定性

4. **packages/components 构建过程补充 CSS 资源复制**：
   - `tsc` 仅输出 JS / d.ts，不会携带 `*.module.css`
   - 为保证 `@fdesign/components` 被 stage 消费时能解析到样式文件，构建脚本追加 CSS 复制步骤，将 `src/**/*.module.css` 同步到 `dist/`

**被否决的替代方案**:
- 继续依赖传递依赖解析 `@tarojs/shared` — 在 pnpm monorepo 下不稳定，已被真实构建错误证伪
- 继续使用 `import styles from '*.module.css'` 并等待运行时自然恢复 — 构建告警表明该消费方式在当前组合下不可靠
- 让 stage 直接 alias 到 components 源码入口 — 会额外引入 TypeScript / barrel 语法兼容问题，且弱化包构建产物验证价值

## 2026-04-03 — 语言规范

**背景**: 治理文档中英混杂，项目人类负责人为中文母语者，审查成本高。

**决策**:
1. 治理文档（AGENTS.md、NORTH_STAR、ROADMAP、ARCHITECTURE、changelog、decisions）统一为中文
2. 代码、变量名、package name 保持英文（行业标准）
3. 代码注释保持英文（行业标准）
4. YAML/JSON 的 key 保持英文（机器解析标准），value 中的描述性文字用中文

## 2026-04-06 — Phase 5 token 协议优先于 provisional 实现

**背景**: 用户提供的 `vars.css` 不仅包含 token 值，还通过备注明确了 radius、spacing、typography 的使用边界；其中部分规则与仓库当前 provisional 实现直接冲突，例如额外 radius 档位、`600/700` 字重、以及若干旧 spacing 刻度。

**决策**:
1. Phase 5 中，`vars.css` 回填后的协议层输入作为设计约束来源，优先级高于仓库当前 provisional token 与组件实现
2. token 实施前，必须先将协议抽取与映射方案固化为仓库文档，并等待用户确认
3. TS token 继续作为多端主源；Web 端可输出 CSS variables 镜像，但不以运行时 CSS 变量替代 TS 主源
4. 不为兼容旧实现而保留被协议明确禁用的 token：
   - 不新增额外 radius 档位
   - 不保留 `600 / 700` 作为正式字体字重 token
5. 若组件当前实现与协议冲突，应调整组件实现或 API，而不是反向放宽协议
6. `Text.weight` 直接收缩 API，不保留 `semibold / bold` 作为对外合同
7. `Card / Modal` 的 padding 协议采用“两层模型”：
   - 外层容器 padding：承担壳层留白
   - 内容行节奏：按内容 archetype 区分纯文字、icon-leading、信息行等间距规则
   - 不再将 `Card / Modal` 映射为单一统一 padding 值
8. 内容行的 `18 / 14 / 12 / 14` 优先解释为“与相邻行元素之间的间距/节奏”，不是无条件叠加在内容行自身上下的 padding
9. 若内容行是外层容器内的首个或最后一个子容器，不得与外层容器的顶部/底部 padding 机械叠加，避免形成双重留白

## 2026-04-06 — Button 按 Figma 节点收缩到真实规格

**背景**: Phase 5 的首个 Figma 组件是 Button。设计稿定义的真实规格与仓库原有 provisional API 不一致：类型从 `primary / secondary / ghost / danger` 收缩为 3 种，尺寸从 `sm / md / lg` 扩展为 6 档，并新增 `inactive` 状态；同时 loading 只有大尺寸展示 spinner。

**决策**:
1. Button 以 Figma 真实规格为准，不继续保留 provisional 变体命名
2. Button 对外变体收敛为：
   - `primary-fill`
   - `primary-outline`
   - `secondary-outline`
3. Button 对外尺寸切换为：
   - `xl / l / m / s / xs / mini`
4. `inactive` 作为独立于 `disabled` 的真实状态进入 Button 对外合同
5. `loading` 仅在 `xl / l` 展示 spinner；其余尺寸保持加载状态语义但不强行添加 spinner

## 2026-04-11 — 新增组件必须优先落到本项目全局样式 token

## 2026-04-23 — Button loading 动画口径纠正

**背景**: 在本轮 Button 收尾中，曾按历史记录把 `loading` spinner 收窄为仅 `xl / l` 显示，但用户明确确认这与当前预期不符。当前要求是 Button 所有尺寸进入 `loading` 状态时都统一显示转动中的 loading 动画，而不是只在大尺寸显示。

**决策**:
1. Button 当前口径以用户最新确认为准，所有尺寸的 `loading` 状态都显示 loading 动画
2. `xl / l / m / s / xs / mini` 在进入 `loading` 时，均保持 loading 语义、不可点击状态与可见 spinner
3. `packages/components`、`packages/adapters`、`apps/stage` 与 Phase 5 治理文档必须同步采用该口径
4. 2026-04-06 关于“仅 `xl / l` 展示 spinner”的历史决策不再作为当前实现依据

## 2026-04-23 — 回滚本轮 Button 治理改动

**背景**: 用户确认本轮 Button 治理改动已破坏当前代码状态，要求回滚到本轮 Button 治理开始之前。

**决策**:
1. 本轮 Button 收尾与后续 loading 口径纠正涉及的代码与治理回写全部撤回
2. 当前 Button 的正式治理口径恢复到本轮 Button 治理开始前的状态
3. 后续若要再次调整 Button，需在用户重新确认目标后再进入新的治理与实现

## 2026-04-23 — 重新确认 Button 当前范围收尾口径

**背景**: 用户重新确认了 Button 当前轮次的治理目标，并要求基于现有 stage 展示方式重新完成治理层收尾。新的确认结论不再是简单恢复回滚前状态，而是以当前代码与 stage 的共同事实为准，重新定义 Base Button 当前范围的正式口径。

**决策**:
1. `Button` 当前轮次的正式范围仍只覆盖多个分类下的 `Base` 类 button；其余 button 分类继续留在后续批次，不在本轮一并宣告完成
2. `Base` 类 Button 的 `loading` 以用户最新确认为准：`xl / l / m / s / xs / mini` 全尺寸进入 loading 时均显示 spinner，并统一保持 loading 语义与不可点击状态
3. `Base` 类 Button 的布局口径以当前 stage 与组件共同收束的结果为准：全部尺寸使用固定高度；`xl` 宽度固定为 `355px`；`l` 宽度约束在 `178px-327px` 范围内，当前 stage 展示按 `327px` 呈现；`xl / l / m` 在 `block` 等流式场景下允许把横向 padding 收缩到最小安全留白
4. 上述第 2-3 条成为当前 Button 收尾与治理回写的正式依据；2026-04-23 “回滚本轮 Button 治理改动”保留为历史事件记录，但不再代表当前终局口径
5. 当 `Base` 类当前范围的组件本体、RN adapter、测试、stage 展示与治理事实源重新一致后，可将 `Button` 当前范围状态收束为“可冻结”

## 2026-04-11 — 新增组件必须优先落到本项目全局样式 token

**背景**: 用户明确要求：后续新增组件的样式变量统一使用本项目设计系统层已定义的全局样式 token；面对设计稿中的硬编码样式值，优先在本项目 token 体系中找到对应值后再使用。若当前 token 暂无可用对应项，可在询问用户确认后临时使用硬编码或 reference token，但必须在当前实现任务收尾前完成语义抽取并回灌到本项目全局 token，再替换组件实现。

**决策**:
1. 新增组件与既有组件后续重构时，样式值只能来自 `@fdesign/tokens` 已定义的全局 token 体系
2. 设计稿中的颜色、字号、圆角、间距、边框、尺寸等视觉样式值，应优先映射到本项目现有全局 token，再由组件消费
3. 若设计稿出现仓库 token 体系尚未覆盖、且当前无法直接对应的值，必须先询问用户确认；确认后才允许临时使用硬编码值或 `reference token` 落地
4. 上述临时落地只允许作为过渡态，且必须在当前实现任务收尾前完成语义抽取：把这些值补入本项目全局 token，再回灌替换组件中的硬编码或 `reference token`
5. 不允许把“临时硬编码 / reference token”长期留在组件实现里作为最终状态
6. 组件私有 CSS 变量仅可作为“承接全局 token 的运行时映射层”，不得成为新的视觉来源；其最终取值仍必须来自本项目全局 token，或处于第 3/4 条约束下的临时过渡态
7. `Button` 作为首个对齐对象，后续需继续清理当前残留的硬编码尺寸/状态值与直接引用 reference token 的实现，收敛到满足本决策的 token 用法

## 2026-04-12 — Phase 5 状态词口径统一

**背景**: 在 Phase 5 转为长期滚动执行后，治理层文档开始同时描述“阶段状态”和“组件批次状态”。若继续混用“已完成 / 首轮完成 / 执行中 / 冻结”等词，会让 `ROADMAP`、`checkpoint` 与 active plan 对同一事实产生不同解读，削弱治理层的单一事实源能力。

**决策**:
1. Phase 5 的状态词分为两层，不再混用：
   - 阶段层：只使用 `进行中`、`已完成`
   - 组件批次层：只使用 `已落地首轮`、`收尾中`、`可冻结`
2. `docs/ROADMAP.md` 只表达阶段层状态，不承担组件批次细节管理
3. `.agent/checkpoint.yaml` 记录当前工作窗口时，可使用组件批次层状态词，但需与 active 主计划保持一致
4. `docs/exec-plans/active/phase-5-token-protocol-and-mapping.md` 作为 Phase 5 唯一长期主计划，负责维护组件批次状态的正式口径
5. 历史文档与历史 changelog 不强制回写为新术语，避免污染历史语境；新进入的治理事实源必须遵循本口径

## 2026-04-14 — Text 不再作为设计系统组件保留

**背景**: 用户确认文字展示应由真实业务需求按多个全局 token 组合生成，而不是围绕一个通用 `Text` 组件的 `tone / size / weight` props 继续扩展。现有 `Text 文本` 页既不能完整表达 typography token 协议，也容易把排版基础能力误叙述为组件 API。

**决策**:
1. `Text` 不再作为 `@fdesign/components` 的设计系统通用组件保留
2. `apps/stage` 中原 `Text 文本` 页重构为 `Typography 排版` 基础验证页
3. `Typography 排版` 归入可折叠一级导航 `CssToken 全局样式`，用于验证字体族、字号、字重、行高和基础文本行为
4. 真实业务页面中的文字由原生文本元素承载，视觉结果由全局 typography token 与页面/组件语义样式组合决定
5. 若后续需要更高层排版抽象，应优先新增语义化封装，而不是恢复一个通用 `Text` 组件 API

## 2026-04-14 — Icon 采用双源统一入口与自动分发

**背景**: 历史 `Icon` 组件仍是 Phase 2 的字符型 placeholder，只能表达极少数语义，既无法承接 Figma 自有图标，也无法兼容用户要求的 Google Material Symbols 线上资源。同时仓库内已有 `info / check / chevron-right` 等旧调用，若直接切断会放大迁移成本。

**决策**:
1. `Icon` 对外采用单一统一入口，新增 `source?: 'local' | 'material'`，但未显式传值时按“local registry 优先，否则 material”自动分发
2. local source 使用仓库内联 SVG registry 承载，图标资源统一收敛到 `packages/components/src/icon/iconRegistry.tsx`，不允许业务页面零散硬编码资源
3. material source 使用 `Material Symbols Rounded` 可变字体，并按既定尺寸协议映射 `wght / GRAD`；字体加载由 H5 消费方负责

## 2026-04-27 — Input / Card / Modal 退出当前公共组件范围

**背景**: 用户明确要求删除 `Input 输入框`、`Card 卡片`、`Modal 弹窗`，并且不只删除组件源码，还要同步删除 stage 中对应的分支页和导航。若仅隐藏页面而保留公共导出，会形成“信息架构已下线、代码面仍可消费”的双轨状态，后续很容易继续被误用。

**决策**:
1. `Input`、`Card`、`Modal` 不再作为 `@fdesign/components` 当前设计系统公共组件保留
2. 对应组件目录、类型、合同、测试、harness 与公共 barrel 导出一并移除，不保留软废弃占位
3. `apps/stage` 中对应页面注册、导航入口与页面文件同步删除，避免 stage 继续传达这三个组件仍在范围内的错误信号
4. `apps/example-consumer` 作为真实消费样板必须同步解耦，确保 workspace build/test 不因残留引用而失效
5. Phase 5 当前默认工作窗口改为继续定义 `Form` 输入类 / 行动类的真实组件范围，而不是继续推进已退出范围的三个旧组件
4. 为兼容现有消费方，旧 icon 名 `info / check / chevron-right` 保留可用，并通过 material source 兜底
5. 当前首轮本地图标集先以内联 SVG 稳定落地；后续若拿到稳定可入库的 Figma SVG/path，再按同一 registry 批量补齐，不在正式组件里长期依赖临时远程 Figma 资产 URL

## 2026-04-21 — 展示类表单以 Form 组件族替代 ListItem

**背景**: 展示类表单的 Figma 节点 `26417:18225` 已明确覆盖单行、多行、刷脸、平铺信息列表、金额列表标题外置、折叠纯文本、折叠金额与聚合多折叠。原 `ListItem` 仅能表达 Phase 2 的单行信息单元，既无法承接折叠/组合型展示，也会继续把组件语义绑定在“列表项”而不是“表单展示”上。

**决策**:
1. `packages/components` 不再保留 `ListItem` 作为公共导出；展示类表单统一由 `form/` 目录下的 `Form` 组件族承载
2. `Form` 组件族对外同时提供命名空间导出 `Form` 与具名导出 `FormRow / FormFaceStatus / FormInfoList / FormAmountList / FormCollapseGroup / FormAggregateCollapseGroup`
3. 折叠类组件统一遵循 `expanded / defaultExpanded / onExpandedChange` 控制口径；jump 统一通过 `onJump` 或 `onAction` 暴露点击接口
4. `apps/stage` 的一级导航从旧 `List Item 列表项` 调整为折叠分组 `Form 表单`，并拆为 `展示类 / 输入类 / 行动类` 三个二级页；本轮仅正式实现 `展示类`
5. 历史消费位若仅依赖旧 `ListItem` 的“标题 + 描述 + 右侧信息”能力，应直接迁移到 `FormRow`，不再保留兼容层

## 2026-04-21 — Stage 预览必须以最新 dist 为真

**背景**: `apps/stage` 通过 workspace 包入口消费 `@fdesign/tokens` 与 `@fdesign/components` 的 `dist`。历史对话已反复暴露“只改 `src`、未同步 `dist`，重启 stage 仍看不到最新效果”的问题；其中 `tokens` 与 `components` 并行构建还会导致依赖顺序错乱，进一步放大误判。

**决策**:
1. `apps/stage` 的运行态验收统一以 workspace 包最新 `dist` 为真，不以 `packages/*/src` 为真
2. 后续 agent 若要在 stage 预览 `tokens/components` 改动，默认使用根命令 `pnpm dev:stage`，由该命令承担“初始串行构建 + 持续监听回灌 dist + 启动 stage”
3. 改动 `packages/tokens/src` 时必须遵守 `tokens build -> components build -> stage 验证` 的串行顺序，不得并行构建 `tokens/components`
4. `packages/tokens` 与 `packages/components` 可各自提供包级 `watch:dist` 作为单包排查入口，但这不替代根级 stage 预览链路
5. 后续凡记录 stage 运行态验证结果，若涉及 `tokens/components` 源码改动，应先确认当前预览来自最新 `dist`，不得只以“重启 `@fdesign/stage` 成功”作为同步依据

## 2026-04-27 — 公共组件 H5 主路径采用可控 Web DOM 优先

背景：NavBar 搜索框在 Taro H5 中使用 Taro `Input` 时会生成 `taro-input-core` / `weui-input` 内层结构，导致视觉还原被平台预设样式干扰。后续 Form 输入类 / 行动类和公共交互组件若继续默认复用 Taro 表单 / 交互组件，会重复踩同类问题。

决策：公共组件 H5 主路径优先输出可控 Web DOM。所有可交互元素的真实节点、状态 class、ARIA 语义与样式命中点都应由 `packages/components` 明确持有；`View` / `Text` 可继续作为 Taro H5 基础布局与文本容器，但真实输入、点击、选择、开关、提交等交互语义不得默认交给会生成额外内层结构或平台预设样式的 Taro 组件。

影响：公共 `Input` 后续需从 Taro `Input` 迁移到组件自持原生 `input`，并保持现有视觉与 `onValueChange` 合同；Form 输入类 / 行动类在定义真实范围前必须先按此路线设计 DOM 结构。未来如需小程序 / RN / Weex 差异实现，通过 adapter、平台分支或独立实现承接，不回退 H5 主路径的 Web DOM 可控性。

## 2026-04-27 — Stage 展示区统一承担可点击节点的 pointer 识别

背景：历史上多次出现“新增组件本身可点击，但 stage hover 没有 pointer”的回归。原因不是单个组件样式漏写，而是 stage 壳层只覆盖了侧边导航，没有把展示区里的交互节点当作统一展示协议治理。

决策：
1. `apps/stage` 作为展示舞台，所有可点击节点在 hover 时必须呈现 `pointer`
2. 该规则属于 stage 私有展示增强，只服务于交互热区识别，不进入组件本体视觉语义
3. stage 壳层默认根据真实交互语义自动命中，包括原生 `button`、`a[href]`、`summary` 与显式 `role="button" | "link" | "tab"`
4. 后续新增组件若 stage 中未出现 pointer，优先检查组件是否缺少真实语义；不要把给某个 demo 单独包 `__stage-clickable` 当作长期主路径

影响：后续 agent 在 stage 中新增或重构组件时，除了看视觉和结构，还必须确认 hover cursor 是否已随真实语义自动生效；这会倒逼组件交互节点持续保持可控 Web DOM 与明确语义

## 2026-04-27 — Stage 页内文本默认允许框选复制

背景：stage 既承担组件展示，也承担文案、token、状态和值的校对工作。若文本不能直接被鼠标框选复制，核对和沟通成本会持续偏高，而且每次新增页面都容易忘记补。

决策：
1. `apps/stage` 页内文本默认允许鼠标框选复制
2. 这条规则属于 stage 私有展示增强，不改变组件本体在真实业务场景中的 `user-select` 语义
3. stage 壳层统一放开文本选择；若真实组件本体为了交互需要保留 `user-select: none`，stage 需要可复制时优先由壳层覆盖，不回写组件本体

影响：后续 agent 在 stage 中新增组件或展示页时，应默认把“文本是否可直接复制”视为壳层体验的一部分，与 pointer 识别一样作为长期展示协议维护

## 2026-04-27 — 组件展示区的 hover pointer 回归组件本体

背景：先前把“可点击皆可 pointer”放进 stage 壳层，初衷是减少展示页漏掉 hover 手形的回归；但 NavBar 标题案例说明，这种兜底会掩盖组件本体是否真正输出了正确的交互语义与 cursor，也会让 stage 体验与真实业务消费体验产生偏差。

决策：
1. 公共组件在 H5 主路径中的真实可点击节点，必须由组件本体自行承担 `cursor: pointer` 与交互语义
2. `apps/stage` 不再为组件展示区统一补 `pointer`；stage 壳层只保留自身导航、折叠组、切换控件等舞台层节点的 pointer 规则
3. stage 对组件展示区的职责改为“运行态验收与问题暴露”，即若 hover 没有 pointer，应优先修组件本体的真实节点、语义与样式，而不是补一层壳
4. `__stage-clickable` 或类似舞台层辅助类不再作为公共组件 hover cursor 的长期主路径

影响：后续新增或重构组件时，hover `pointer` 将成为组件本体交付标准的一部分，与真实 DOM、ARIA 语义、状态 class 一起由 `packages/components` 持有；stage 继续作为验收场，而不再扮演交互光标兜底层。

## 2026-04-27 — Phase 5 治理事实源收拢

背景：`8ff83d4` 合入后，仓库同时发生了三类结构性变化：`NavBar` 新增并完成首轮 Figma 对齐，`Input / Card / Modal` 退出当前公共组件范围，stage hover `pointer` 规则从“壳层统一兜底”修正为“组件本体承担、stage 负责验收”。这些变化已分散记录在 checkpoint、changelog 与 active plan 中，需要统一事实源边界，避免后续 agent 继续按旧组件面或旧 pointer 规则推进。

决策：
1. `docs/ROADMAP.md` 只记录阶段层状态和历史范围演进，不承载 NavBar 每个修复点的细节
2. `docs/exec-plans/active/phase-5-token-protocol-and-mapping.md` 继续作为 Phase 5 唯一 active 主计划，负责维护组件批次状态、长期协议与滚动推进顺序
3. `.agent/checkpoint.yaml` 只记录当前工作窗口和短期事实快照，不再复制 active plan 中的全部长期协议
4. `NavBar` 纳入 Phase 5 “已落地首轮”组件批次；后续只接受新 Figma 输入、真实业务消费反馈或平台适配反馈驱动的增量修正
5. `Input / Card / Modal` 退出当前公共组件范围的口径同步到 ROADMAP 与 active plan；如未来重新进入，必须重新定义真实业务语义、Web DOM 边界与 token 映射，不恢复 Phase 2 旧实现作为默认路径
6. `Stage 展示区统一承担可点击节点的 pointer 识别` 是已被后续决策修正的历史记录；当前有效口径以 `组件展示区的 hover pointer 回归组件本体` 为准

影响：后续治理层更新应优先判断信息属于“阶段口径、长期主计划、当前工作窗口、历史变更记录”中的哪一类，再写入对应文件；重复的中间态记录不再继续扩散到多个事实源。
