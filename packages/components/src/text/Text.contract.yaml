component: Text
purpose: "通用文本展示组件，用于承载语义化层级之外的视觉文案"
provisional: true
forbidden:
  - "不允许在 Text 内部承载业务格式化逻辑"
  - "不允许硬编码颜色值，必须使用 @fdesign/tokens"
a11y:
  - "截断仅影响视觉展示，不应隐藏原始文案含义"
slots:
  children: "文本内容"
states:
  - default
  - truncated
