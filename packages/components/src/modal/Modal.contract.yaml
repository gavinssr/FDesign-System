component: Modal
purpose: "轻量模态层容器，用于承载确认、提示和补充操作"
provisional: true
platform_notes:
  h5: "当前为页面内渲染模型，后续如需 Portal 可在不破坏 API 的前提下演进"
  rn: "层级与手势处理需要 Phase 4 adapter 支持"
forbidden:
  - "不允许在 Modal 内部耦合业务提交流程"
  - "不允许硬编码颜色值，必须使用 @fdesign/tokens"
a11y:
  - "open 时渲染 role=dialog 与 aria-modal=true"
  - "关闭操作应始终可达"
slots:
  children: "模态主体内容"
states:
  - closed
  - open
