component: ListItem
purpose: "列表场景的基础信息单元，支持前后置区域与元信息"
provisional: true
forbidden:
  - "不允许在 ListItem 内部耦合业务路由或埋点行为"
  - "不允许硬编码颜色值，必须使用 @fdesign/tokens"
a11y:
  - "disabled 时设置 aria-disabled=true"
  - "可点击模式下应暴露 button 语义"
slots:
  leading: "前置图标或头像"
  trailing: "后置操作或提示"
states:
  - default
  - disabled
  - interactive
