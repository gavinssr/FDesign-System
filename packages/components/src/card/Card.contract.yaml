component: Card
purpose: "通用内容容器，用于组织标题、描述和主体信息块"
provisional: true
forbidden:
  - "不允许在 Card 内部耦合业务跳转逻辑"
  - "不允许硬编码颜色值，必须使用 @fdesign/tokens"
a11y:
  - "interactive 模式下应暴露可点击语义"
slots:
  children: "卡片主体内容"
states:
  - default
  - interactive
