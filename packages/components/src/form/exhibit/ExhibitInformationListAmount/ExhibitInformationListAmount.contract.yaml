component: form/exhibit/ExhibitInformationListAmount
purpose: "折叠信息列表（金额），card=true/false × expanded 双态；card 展开态子行自动切换为 jump"
provisional: false
figmaNode: "8275:8513"
forbidden:
  - "禁止内联颜色/字号；必须通过 @fdesign/tokens 与 spacing.component.exhibit"
  - "禁止在此 cell 内引入 hover 态样式与默认 cursor: pointer（移动端协议）"
  - "禁止对外导出 parts/*"
states:
  - card=true
  - card=false
  - expanded=true
  - expanded=false
  - controlled
  - uncontrolled
tokens:
  parts:
    - FoldingPureHeader
    - SubListFlush
